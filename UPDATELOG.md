# 更新日志

每次发版请同步维护本文件与当天 `WORKLOG_YYYY-MM-DD.md`，并同步更新前端展示使用的 `client/public/update-log.json`。更新日志标题请带上版本号。

## 2026-05-04 · 1.10.40 · 首页销售卡片实时刷新

- 影响范围：首页销售额、首页销售日历、移动端
- 关键更新：
  - 首页销售额卡片保留 15 秒静默实时刷新，并在从日历切回销售额时立即重新拉取当前销售额数据。
  - 首页日历卡片新增 15 秒静默实时刷新，停留在首页时会持续更新日历销售额、活跃天数和商品件数。
  - 点击标题或移动端左右滑动切换销售额/日历时，目标卡片都会立刻刷新，避免切换后看到旧数据。
  - 刷新过程保持静默，不改变现有卡片切换动画、点位筛选、月份选择和深色模式样式。

## 2026-05-02 · 1.10.39 · 首页销售日历与日历下钻

- 影响范围：首页、销售记录、销售日历、移动端、深色模式
- 关键更新：
  - 首页销售额卡片新增销售日历卡片，同一卡片位置通过点击主标题切换；切换改为轨道式横向平移，避免半透明卡片叠加透底和收尾闪烁。
  - 移动端首页卡片支持左右滑动切换：左滑进入日历，右滑返回销售额，纵向滚动不受影响。
  - 首页销售额点位切换入口从主标题迁移到副标题，主副标题恢复上下排版；日历卡片标题、副标题样式与销售额卡片保持一致。
  - 销售记录页“销售日历”更名为“日历”，新增可点击副标题切换店铺范围，调用系统选择框。
  - 修复销售日历在已筛选店铺或销售员后只能看到人员总额、无法继续进入订单明细的问题；筛选和未筛选下钻层级保持一致。

## 2026-05-01 · 1.10.38 · 系统选择框与首页设置持久化

- 影响范围：首页销售额设置、全局选择框、录入销售、工单管理、移动端、深色模式
- 关键更新：
  - 修复首页销售额设置保存后重新进入仍按默认“本月”初始化的问题；销售额偏好现在会在首页图表首次渲染前读取并传入图表组件。
  - 新增全局系统选择框兼容层，保留原 `.el-select` 外观与 `v-model`、`change`、`clearable`、`disabled` 等常用交互，菜单调用系统原生选择框。
  - 工单页面按要求回退 Element Plus 原选择框；录入销售页面改为系统选择框。
  - 系统选择框样式沿用现有主题变量，补齐移动端字号、禁用态、清空态和深色模式表现。

## 2026-05-01 · 1.10.37 · 首页销售额图表范围与设置

- 影响范围：首页销售额图表、销售摘要接口、移动端、深色模式
- 关键更新：
  - 首页销售额卡片恢复“销售额”主标题，副标题调整为“点位 · 时间”格式，例如“总 · 本月”“梦时代 · 本月”。
  - 点击“销售额”标题可用系统选择框切换销售范围，管理员可选总销售额或店铺，单店账号默认并限制在自身店铺范围。
  - 点击右侧销售额金额可打开销售额设置，支持切换门店/维修中心销售额、默认时间、默认对比方式和默认点位；未保存返回会二次确认。
  - 首页图表的 Y 轴与对比金额改为 `k / w` 简写，卡片内边距与排班、工单卡片保持一致。
  - 销售摘要接口新增环比同期与同比对比窗口，销售记录页图表保持原有交互与展示不变。

## 2026-04-29 · 1.10.36 · 页眉通知面板切换重做

- 影响范围：页眉设置/通知、通知时间展示、深色模式
- 关键更新：
  - 清理此前针对页眉通知面板的多层事件托底，包括页签 `pointerdown/mousedown/touchstart` 重复绑定、内部指针锁、document 捕获关闭和页签延迟恢复任务。
  - 页眉设置浮层改回更直接的交互模型：设置/通知页签只由按钮自身 `click` 切换，点击透明背景才关闭，卡片内部点击不参与外部关闭。
  - 修复通知列表中字符串时间直接传入 `Intl.DateTimeFormat` 造成的渲染异常，避免有真实通知时切到通知页后面板崩溃或回到设置内容。
  - 已在本地 mock API 下复测设置 -> 通知、通知 -> 设置 -> 深色模式 -> 通知、点击卡片外关闭，通知面板可稳定显示并正常操作。

## 2026-04-28 · 1.10.35 · 工单分配表头固定与库存数量展示

- 影响范围：工单管理、工单分配、库存展示、深色模式
- 关键更新：
  - 工单管理 > 分配 > 分配编辑表格新增固定标题行，纵向滚动商品与点位分配内容时，表头会保留在可视区域。
  - 商品名称列中的数量说明从“原单数量”调整为“原单/库存数量”，库存读取当前发货仓库/店铺的实时发货库存。
  - 例如进货单原单数量为 `10`、发货库存为 `13` 时，商品行显示 `原单/库存数量 10/13`。
  - 分配表格二级表头文案从“本次变动库存 / 当前库存”精简为“变动库存 / 库存”，库存异常提示同步改为“库存”。
  - 固定表头样式使用现有主题变量，已复查深色模式下表头背景、边线和输入框对比度。

## 2026-04-28 · 1.10.34 · 页眉通知浮层关闭链路重构

- 影响范围：页眉设置/通知、Safari 与移动端兼容、深色模式
- 关键更新：
  - 继续修复正式环境中点击页眉“通知”后浮层立即关闭的问题。
  - 透明全屏层不再参与点击命中，避免 Safari/移动端在页签切换后把合成事件落到外层。
  - 外部点击关闭改为 `document` 捕获阶段判断，只有点击真正落在设置按钮和浮层之外时才关闭。
  - 菜单内部补齐 `pointerdown`、`mousedown`、`touchstart` 保护，设置/通知页签切换在 Safari 和移动端事件序列下保持打开。
  - 本轮只修改页眉浮层事件链路和版本日志，未触碰其他业务功能，深色模式样式保持现有变量。

## 2026-04-27 · 1.10.33 · 页眉通知切换不再被外层点击误关

- 影响范围：页眉设置/通知、Safari 兼容、深色模式
- 关键更新：
  - 继续修复正式环境中页眉设置浮层点击“通知”后菜单被关闭的问题。
  - 线上 Safari 日志确认 `/api/notifications` 已触发，问题集中在页签切换后的合成 click 被透明外层接收并关闭浮层。
  - 透明外层现在只通过真正的外部 `pointerdown` 关闭，去掉易误伤内部切换的 `click` 关闭兜底。
  - 设置按钮再次点击关闭、Esc 关闭和外部点击关闭能力保持不变，深色模式样式未调整。

## 2026-04-27 · 1.10.32 · 页眉设置面板切换稳定与本地测试账号更新

- 影响范围：页眉设置/通知、本地测试系统、深色模式
- 关键更新：
  - 页眉设置浮层的“设置 / 通知”页签切换新增最后一次选择保护，上一轮切换留下的延迟恢复任务不再覆盖当前选择。
  - 关闭页眉浮层或组件卸载时会同步清理页签切换定时器，避免后续打开时状态串扰。
  - 已在本地测试系统实测“设置 -> 通知 -> 设置”来回切换和快速切换，最后选择的面板能够稳定保留。
  - 本地测试系统账号配置已更新，生产环境仍不会显示本地测试登录入口。

## 2026-04-27 · 1.10.31 · 导出金额去尾零与移动端工单层级收口

- 影响范围：店铺/仓库商品导出、销售录入、页眉通知、移动端工单、深色模式
- 关键更新：
  - 店铺/仓库管理 > 商品 > 导出表格中，单价、金额和合计金额改为去掉无意义 `.00`，超过千元也与千元以下保持一致；真实小数仍会保留。
  - 销售录入表单拦截 Enter 提交，移动端键盘“确定”只收起当前输入框键盘，不再误触提交销售记录。
  - 页眉设置里的“设置 / 通知”页签切换去掉重复原生绑定，改由按钮自身在 `pointerdown/click` 阶段稳定切换通知面板。
  - 从全局搜索商品分布创建商品调拨单后，会重置移动端业务弹层层级；工单选品、批量录入、销售选择弹窗和商品联想菜单保持在新建工单页之上。
  - 提示消息与确认框继续高于业务弹层，深色模式沿用现有变量和样式。

## 2026-04-24 · 1.10.30 · 页眉通知遮罩点击再收口

- 影响范围：页眉设置/通知、深色模式
- 关键更新：
  - 继续修复页眉设置浮层中点击“通知”后整张卡片被关闭的问题。
  - 设置浮层的透明遮罩和菜单卡片改成兄弟层，外部点击只由透明 backdrop 负责关闭，菜单内部点击不再经过关闭层。
  - 设置/通知页签的 `pointerdown` 只负责锁定内部交互，真正切换回到 `click` 执行，避免按下瞬间换内容导致释放 click 被浏览器落到遮罩上。
  - 通知加载补上全局防重入和失败兜底，避免同一次点击或轮询造成重复请求、异常后 loading 卡住。
  - 未调整页眉卡片结构、视觉样式和深色模式变量。

## 2026-04-24 · 1.10.29 · 页眉通知切换按下即生效

- 影响范围：页眉设置/通知、深色模式
- 关键更新：
  - 修复页眉设置浮层中点击“通知”后无法稳定切换到通知面板的问题。
  - “设置 / 通知”页签现在会在 `pointerdown` 阶段先完成面板切换，避开浏览器 click 合成和外层浮层关闭链路的竞争。
  - 保留 `click` 兜底，键盘 Enter/Space 仍可切换页签；通知加载增加防重复保护，避免一次点击触发两次通知请求。
  - 本轮不调整卡片结构和 CSS，深色模式继续沿用现有变量与样式。

## 2026-04-23 · 1.10.27 · 扫码确认弹层滚动收口与桌面二维码信息精简

## 2026-04-23 · 1.10.28 · 扫码登录记住策略对齐与二维码刷新按钮收口

- 影响范围：统一登录回跳、桌面端扫码登录、退出登录、深色模式
- 关键更新：
  - AQC 登录态存储补齐为 `localStorage / sessionStorage` 双模式，账号中心回跳时会按本次登录的“记住登录”结果决定是否持久化。
  - 勾选“仅登录一次”的桌面扫码登录现在只保存在会话级存储里，关闭桌面浏览器后不会再自动恢复登录。
  - 使用账号密码登录并回跳 AQC 时，会继续保留持久登录能力，下次打开可自动恢复登录。
  - 桌面端二维码页的“刷新二维码”按钮改为更轻的次级按钮，尺寸缩小并水平居中，与下方按钮组样式统一。

- 影响范围：移动端扫码登录、桌面端二维码登录、深色模式
- 关键更新：
  - 移动端页眉“扫码登录”的上滑菜单恢复为可继续上拉展开，弹层内容区也重新允许纵向滚动，不再出现确认区被底部卡住、无法滑动到按钮位置的问题。
  - 移动端扫码二次确认界面已精简：移除了“目标服务”“扫码时间”等信息卡片，也去掉了“请确认是否允许……”这类冗长提示，仅保留确认动作和“仅登录一次”说明。
  - 桌面端二维码登录页移除了顶部“等待手机扫码/等待手机确认登录”独立状态卡，以及“目标服务 / 登录设备 / 扫码时间”信息卡，视觉更简洁，确认状态仍保留在二维码主体区域内。

## 2026-04-23 · 1.10.26 · 扫码登录二次确认、工程师角色回写与页眉通知修复

- 影响范围：移动端扫码登录、页眉设置/通知、账号角色同步、深色模式
- 关键更新：
  - AQC 移动端“扫码登录”改成先扫码、再手机二次确认；扫码成功后会进入确认面板，可明确看到目标服务、登录设备，并支持“确认登录 / 取消”。
  - 新增“仅登录一次”开关，默认勾选；确认时会把该参数一并透传给账号中心，勾选后桌面端本次登录不会记住密码，也不会自动登录。
  - 页眉设置里的“通知”切换补强了浮层点击拦截链路，通知页签切换时不再被外层关闭逻辑误判收起。
  - AQC 服务端新增扫码“已扫描待确认”和取消代理接口，账号中心桌面端扫码后会先进入“等待手机确认登录”，可在桌面端主动取消。
  - 工程师角色同步链路已补齐：账号中心支持 `aqc_engineer / aqc_manager / aqc_departed`，后续登录不会再把工程师身份错误回写成销售员。

## 2026-04-22 · 1.10.22 · 通知同卡切换、工单空态与排序落盘

## 2026-04-22 · 1.10.23 · 工单设置替换定时开单与确认弹窗再抬层

## 2026-04-22 · 1.10.24 · 连续新增栏上移与首行插入

## 2026-04-22 · 1.10.25 · 工单排序保存同步到查看与审批

- 影响范围：工单编辑、工单详情、工单审批、深色模式
- 关键更新：
  - 工单编辑中对明细表格做列排序后，保存草稿或提交审批时，现在会先把当前排序结果真正写回明细数组，再按该顺序保存，不再只是在编辑表格里临时显示排序。
  - 工单详情和审批面板现在会按持久化的明细排序顺序恢复显示，查看工单和审批工单时将与编辑保存后的顺序保持一致。
  - 后端已将工单明细的持久化排序字段显式回传给前端，前端在重新打开编辑、查看详情、提交审批、审批通过/驳回后的回写链路里都会统一按该顺序标准化。
  - 销售换货单的换入/换出明细也一并纳入相同顺序回放逻辑，避免只在编辑态看见排序，详情态又回到旧顺序。

- 影响范围：工单录入、桌面端工单明细、深色模式
- 关键更新：
  - 桌面端工单明细中的“底部连续新增栏”已上移到表格上方，并统一改名为“连续新增栏”，录入提示文案同步更新。
  - 通过连续新增栏录入表格中尚不存在的商品时，系统不再写入表格尾部，而是始终以“新商品插入第一行”的方式落表，和之前尾部追加逻辑相反。
  - 如果连续新增栏录入到表格里已存在的商品，仍然继续沿用原有重复商品合并逻辑，只把数量 `+1`，不新增新行。
  - 上一版为底栏录入补的“固定锚点从下往上”和“录入后自动下滑动画跟随”这两条行为已取消，避免桌面端上移后再出现视口被强制牵引的问题。
  - 移动端连续新增栏保留原位置与录入方式，但同样改为新商品优先插入到表格第一行，视觉与深色模式样式继续沿用现有变量。

- 影响范围：工单设置、工单录入、移动端调拨确认、深色模式
- 关键更新：
  - “定时开单”功能已下线，入口改为“工单设置”；现在可以按工单类型单独设置默认负责人，新建工单时会自动带入，商品调拨单默认负责人已预设为柏云。
  - 工单编辑中的底部连续新增栏改为按真正的移动端底部弹层容器滚动，连续录入时会以底部为锚点保持新增栏留在视口内，减少表格越加越高后底栏被顶出屏幕的问题。
  - 移动端从“全局搜索 -> 商品分布 -> 创建商品调拨单”进入工单后，如再次修改触发二次确认，确认框和遮罩都会统一抬到业务弹层之上，修复确认弹窗仍被压在后面无法点击的问题。
  - 后端已新增工单默认负责人设置模型与接口，工单元数据会返回各类型默认负责人；提交草稿或送审时如果未手动选择负责人，会优先尝试回填对应类型的默认负责人。
  - 工单定时任务执行器已停止启动，不再自动创建计划工单；工单入口、默认负责人设置和深色模式样式保持现有结构不变。

- 影响范围：页眉通知、工单明细、工单分配、移动端调拨确认、深色模式
- 关键更新：
  - 页眉设置里的“通知”已回到同一张设置卡片内切换，不再额外弹出独立通知窗口；点“通知”后会直接在原卡片中切换内容，设置卡片尺寸与切换栏样式保持不变。
  - 全局搜索进入“商品分布 -> 创建商品调拨单”后的二次确认弹窗，统一抬高到应用级浮层之上，修复移动端确认框被压在后面无法点击的问题。
  - 新建工单默认不再自动生成空白明细行；当明细为空时，表格会直接提示“点击新增一行、批量录入，或使用连续新增栏开始录入”。
  - 工单编辑中点击列排序后，保存提交会按当前排序后的行顺序落盘，不再回到排序前的原数组顺序。
  - 工单分配里的“商品名称”列已补上和其他工单表一致的排序能力。
  - 使用底部连续新增栏连续录入时，弹窗内容区会跟随底栏一起下滑，减少录入后底栏脱离当前视口的问题。

## 2026-04-22 · 1.10.21 · 连续新增栏去空白补行与通知入口回收

- 影响范围：页眉通知、工单录入、深色模式
- 关键更新：
  - 底部连续新增栏录入商品后，不再额外在表格尾部补出一条新的空白行，保持表格现有行结构不被持续拉长。
  - 连续新增栏命中表格里已存在的同商品时，会直接沿用原本的重复商品合并逻辑，只把该商品数量 `+1`，不再额外新增一行。
  - 页眉独立通知按钮已取消，未读红点与通知入口收回到设置按钮里；通知仍通过设置面板顶部“通知”切换进入，继续沿用当前稳定的承载方式，避免回到之前点不开的问题。

## 2026-04-22 · 1.10.20 · 页眉通知独立面板与工单底部连续新增栏

- 影响范围：页眉通知、工单录入、进货工单、深色模式
- 关键更新：
  - 页眉通知从容易互相干扰的设置浮层中拆成了独立通知面板，页眉新增单独的通知按钮；设置面板顶部仍保留“设置 / 通知”切换栏与细金框当前态，但点击“通知”会稳定打开独立通知面板，不再出现一点就关闭的问题。
  - 独立通知面板继续复用原有通知列表、报告查看、群组邀请处理和浏览器通知开关，移动端与深色模式沿用现有卡片变量，不额外分叉样式。
  - 工单主表底部新增常驻“连续新增栏”，支持输入条码或商品名称，复用现有联想补全与条码快速匹配；匹配成功后会把商品自动写入上方表格，并保持输入焦点留在底栏，方便连续扫码或连续录入。
  - 连续新增栏会优先占用现有空白行，不够时自动补行，并继续保证表格里始终保留至少一条可编辑空白行，减少对原有录入节奏的影响。
  - 进货工单的品牌列已改成可编辑联想输入，不再是只读固定值；品牌、系列和商品自动填充之间继续保持兼容。

## 2026-04-22 · 1.10.19 · 通知遮罩收口与工单守焦续修

- 影响范围：页眉通知、工单录入、深色模式
- 关键更新：
  - 页眉“设置 / 通知”浮层不再依赖 document 级别的外部点击判断，改为浮层自身遮罩负责点击外部关闭，修复 Safari 下点击“通知”页签或通知区域时被误判为外部点击、整张卡片直接关闭的问题。
  - 通知浮层内部继续保留原有切换栏与细金框当前态，样式和深色模式变量不变，仅收口关闭机制。
  - 工单补全跳转在原有“同步补行、延后落焦”基础上，又补了一层短时间守焦，会在联想控件或输入框把焦点抢回去时再次拉回目标行对应字段。
  - 自动跳转前会先释放当前活跃输入框的焦点，再把光标落到下一排目标输入框，减少商品联想、条码识别和订单匹配后的焦点回弹。

## 2026-04-22 · 1.10.18 · 工单补行恢复与通知点击开关修复

- 影响范围：工单录入、页眉通知、深色模式
- 关键更新：
  - 工单页已撤回 `1.10.17` 那批“请求去重 + 排队推进”的录入改动，恢复原本先同步判断并补出下一行的路径，末排录入后会再次自动补出一行空白输入位。
  - 商品联想、条码识别、订单号匹配和选品弹窗带入后，改成“先确定目标行并立即补行，再延后到控件收口后落焦”，避免 Element Plus 联想输入框把光标抢回当前行。
  - 商品名称列重新补上回车即提交精确匹配，并在联想面板展开时跳过这条提交链路，兼顾手输回车与联想选择两种录入方式。
  - 页眉“设置 / 通知”面板改为纯点击打开、点击外部关闭，不再混用 hover 收口，修复点击通知页签或通知入口时整张卡片被立即关闭的问题。
  - 顶部切换栏沿用细金框当前态，深色模式继续复用现有主题变量，不额外分叉。

## 2026-04-22 · 1.10.17 · 通知页签收口、工单跳转稳态与移动端调拨弹层修复

- 影响范围：页眉通知、工单录入、移动端全局搜索、深色模式
- 关键更新：
  - 页眉设置卡片里的“设置 / 通知”当前项补上了和主题模式一致的细金框选中态，同时将 hover 收起改成“离开后再二次确认是否仍悬停在卡片区域”，继续收口通知页签切换偶发被误关的问题。
  - 工单明细在商品联想、条码识别和订单号匹配后的自动跳转补上了同值请求去重，并把推进下一行改成延后到本轮输入事件完全结束后再执行，减少回车与 blur/change 竞争导致的重复请求和丢焦。
  - 移动端从“全局搜索 -> 商品分布 -> 创建商品调拨单”进入新建工单前，会先完整关闭搜索、分布和操作面板，再切入工单页面；同时统一抬高 `el-select / el-autocomplete / 日期选择` 弹层层级，修复该链路下选项和联想窗口被遮住的问题。
  - 以上样式与浮层层级一并复核了深色模式，保持现有主题变量和卡片质感不分叉。

## 2026-04-21 · 1.10.16 · 工单方向键导航与通知浮层续修

- 影响范围：工单、页眉通知、深色模式
- 主要更新：
  - 工单明细的自动聚焦继续补强，自动跳转现在会优先筛选“可见、可编辑、可聚焦”的真实输入框，并追加双 `requestAnimationFrame` 与延后补焦，减少条码扫描、商品带入、订单匹配后仍落回旧单元格的情况。
  - 工单编辑表格补上了统一的方向键导航；在没有打开联想或下拉菜单时，`↑ ↓ ← →` 现在可以在同一张表内切换到相邻可编辑单元格，条码列、数量列和已自动补全后的文本列都可直接连续录入。
  - 页眉设置浮层的“通知”页签继续收口：页签切换在 `pointerdown` 阶段就会先锁定 sticky，锚点与浮层之间的移入移出也改成互相识别，修复鼠标跨过间隙或点击页签时整张卡片被误关的问题。

## 2026-04-21 · 1.10.15 · 工单跳转稳态修复与通知页签收口

- 影响范围：工单、页眉通知、深色模式
- 主要更新：
  - 工单明细的自动聚焦从简单查询改成字段注册 + 可见输入判断，并补上更长的重试链路，条码扫描、商品联想、订单带入和分页切换后的下一行跳转会更稳定。
  - 商品名称列现已支持在直接输入后按回车或失焦自动精确匹配现有商品；匹配成功后会补全整行并自动跳到下一行同字段，减少必须手点联想项的中断。
  - 销售类订单号列补上了回车/失焦后的唯一精确匹配；已匹配完成的订单行再次回车时，也会直接跳到下一行订单号输入框。
  - 页眉设置浮层现在会在卡片内交互时自动转为 sticky，并改用更稳的事件路径判断与更宽松的悬停关闭延时，修复点击“通知”页签仍把整张卡片关掉的问题。

## 2026-04-21 · 1.10.14 · 工单跳转续修与页眉通知开关收口

- 影响范围：工单、页眉通知、深色模式
- 主要更新：
  - 工单明细中的条码列现已补上回车触发匹配，条码识别成功后会立即尝试跳到下一行同列；同时自动聚焦改成更强的延迟重试，减少 Element Plus 输入框异步渲染导致的丢焦。
  - 当录入到相同商品需要合并时，系统不再直接删除当前行，而是把数量合并到已有行后保留当前空白输入行，避免下方可录入行数减少。
  - 清空商品行的逻辑改为按当前工单类型重建一条同位置的新空白行，连同行默认店铺、销售员、数量与渠道一起恢复，便于继续连续录入。
  - 页眉设置面板的外部关闭监听已改成 `pointerdown` 链路，并为面板和顶部“设置 / 通知”切换补上事件拦截，修复点通知页签时整个卡片被立刻关闭的问题。

## 2026-04-21 · 1.10.13 · 工单自动跳转补强与页眉切换回滚

- 影响范围：工单、页眉通知、深色模式
- 主要更新：
  - 工单明细中通过条码、商品联想或订单选择自动带入整行内容后，现会更稳定地跳转到下一行对应字段；订单号列与条码列都已补齐焦点标记和重试聚焦。
  - 保存工单到草稿时，如果存在空白行，会先询问是否自动删除；确认后会在保存前统一清理空白行。
  - 页眉设置卡片顶部新增紧凑的“设置 / 通知”二分切换，默认停留在设置；切到通知时会在同一载体中显示通知内容，卡片大小不变。
  - 主题模式样式已回滚为之前一排三种紧凑按钮的布局，并继续保持与现有设计语言一致。

## 2026-04-21 · 1.10.12 · 工单录入自动跳转合并与页眉面板归并

- 影响范围：工单、页眉通知、深色模式
- 主要更新：
  - 草稿工单详情卡片底部已新增“提交审批”按钮，申请人可在查看页直接把草稿或驳回工单送审，不必先进入编辑。
  - 工单明细在通过商品名称/型号联想、条码识别或选品弹窗带入商品后，会自动跳到下一行相同字段；若当前已经是最后一行，则会先自动新增一行再跳转。
  - 当同一明细区中连续录入到相同商品时，系统会自动合并成一行，并把数量累加，减少重复行。
  - 页眉通知与设置已重新合并为同一个入口，未处理红点保留在设置按钮；打开后可在顶部卡片区直接切换“通知栏”板块，视觉尺寸与主题模式卡片保持一致。

## 2026-04-21 · 1.10.11 · 分配分页收口与页眉通知触发修复

- 影响范围：工单分配、页眉通知、深色模式
- 主要更新：
  - 工单分配编辑页现已补上分页，默认每页 `20` 条，并支持切换为 `10 / 50 / 100` 条，长商品单据的分配录入会更稳定。
  - 分配表格中的“本次变动库存”已改成纯数字输入框，移除了两侧 `+ / -` 按钮和浏览器默认上下箭头，避免误触和样式干扰。
  - 分配设置会自动记忆上一次选择的收货店铺 / 仓库及其顺序；下次进入其他已审批工单分配时会优先带出，并继续按可用点位自动过滤。
  - 分配编辑已移除库存数量、单价、金额三列，单价改为直接显示在商品名称单元格内；底部取消按钮也已去掉，整体布局与现有工单页继续保持一致。
  - 页眉通知按钮已改为直接触发的独立按钮实现，未处理红点不再包裹在额外徽标层里；同时补强了按钮层级与命中区域，修复点击和悬停仍无法呼出通知面板的问题。

## 2026-04-21 · 1.10.10 · 工单分配上线、页眉通知修复与账号身份同步校正

- 影响范围：工单、页眉通知、管理员设置、账号中心同步
- 主要更新：
  - 修复工单编辑中的批量导入残留选择问题，重新打开批量商品/销售记录弹窗时会清空上次选择，避免重复带入旧记录。
  - 工单明细表格的操作列标题新增“删空白”，可按当前页快速清理空白行；普通明细、销售换货换入与换出表格均已接入。
  - 页眉设置不再复用通知红点，系统只在通知按钮显示未处理红点；通知面板也已改回稳定的独立浮层，桌面端悬停与点击都能正常呼出。
  - 管理员设置修改用户身份后，现会同步更新本地账号角色、权限口径与 `account-symuse` 侧身份组，修复工程师被刷新回销售员、身份名称映射错误的问题。
  - 已审批的商品进货单与商品调拨单新增“分配”入口，可先选择负责人和分配点位，再按点位录入数量并保存分配草稿；确认后会自动生成对应的商品调拨单并放入草稿箱，且默认沿用原工单共享群组。
  - 分配编辑补上未分配、超原单、超库存等二次确认提醒，并保持深色模式与现有工单页面风格一致。

## 2026-04-21 · 1.10.9 · 页眉通知重做、工单总计补齐与业绩排名改版

- 影响范围：页眉通知、工单详情、报告中心、移动端样式
- 主要更新：
  - 页眉通知已改为更稳定的弹出层实现，交互与页眉设置保持同级，桌面端悬停和点击都可正常打开通知面板。
  - 工单查看详情中的普通工单表格、销售换货单换入明细与换出明细，已在分页下方补上总数量和总金额，避免只看到当前页合计。
  - 工单编辑页原有的总合计展示继续保留，与表格中的当前页合计形成双层核对，便于检查大单据。
  - 报告中心的业绩排名详情已在移动端改成参考商品分布的无卡片上滑布局，桌面端详情宽度也同步增加，长列表查看更舒展。
  - 深色模式下的通知面板、工单总计标签和业绩排名详情一并跟随现有主题变量适配。

## 2026-04-21 · 1.10.8 · 工单并发稳定性补强与连接池扩容

- 影响范围：工单、通知中心、数据库连接池、服务器稳定性
- 主要更新：
  - 在复核 `1.10.7` 上线后的线上日志时，定位到 API 存在 `QueuePool limit of size 5 overflow 10 reached` 的数据库连接池超时；现已将 SQLAlchemy 连接池改成显式配置，并把默认容量提升到 `pool_size=12`、`max_overflow=24`。
  - API 同步补上 `DB_POOL_TIMEOUT` 与 `DB_POOL_RECYCLE` 配置，保持现有长连接回收与失活探测逻辑，降低工单列表、通知、首页并发请求时的连接争抢风险。
  - `docker-compose.yml` 已暴露新的连接池参数，后续如果线上负载继续变化，可直接通过 `.env.docker` 调整，不需要再改代码。
  - 结合数据库容器内存、1000 条工单压测和最近超时日志，本轮结论仍是不直接解除 `aqc-n-db` 内存限制，优先通过后端实现优化与连接池扩容收口并发稳定性问题。

## 2026-04-18 · 1.10.5 · 报告调度独立配置与周报月报撤回

- 影响范围：报告中心、管理员设置、报告调度、通知撤回
- 主要更新：
  - 报告设置已改为按日报、周报、月报三张卡片分别配置，周报可单独选择每周几的推送 / 清理时间，月报可单独选择每月几号的推送 / 清理时间，全部按北京时间执行。
  - 后端报告调度已改为严格按各周期自己的时间规则判断是否到点，不再继续沿用日报的统一时间去驱动周报和月报。
  - 报告通知清理现已改成按周期独立撤回，只会撤回对应周期的通知，不会再把当日所有报告通知一起清空。
  - 已在线撤回此前误推的所有周报和月报通知与报告记录，并重置对应调度游标，避免这批误推内容继续对所有成员可见。
  - 管理员设置中的报告设置入口已同步升级为同一套独立调度表单，继续兼容深色模式与现有权限逻辑。

## 2026-04-18 · 1.10.4 · 报告中心库存分布收口与日志中心独立页眉

- 影响范围：报告中心、日志中心、报告推送、数据库迁移
- 主要更新：
  - 报告中心中的库存分布现已改成与库存变动明细等高的固定高度详情卡，内部滚动显示；销售员排行榜也同步改成固定高度，并支持点击销售员后打开销售详情弹窗。
  - 库存报告的库存分布已统一改为按新口径统计店铺、仓库、其他仓与维修点；周报、月报和旧报告详情在打开时会按当前代码自动重建 payload，不再继续沿用旧结构里只包含店铺的数据。
  - 修复库存分布各点位净变动始终显示 `-` 的问题，后端现已补齐点位 `shopId / shopType / netChangeQuantity / changeInQuantity / changeOutQuantity`，并统一带入日报、周报和月报。
  - 修复月报测试推送 500，根因是报告日志 `payload_json` 超出 MySQL `TEXT` 容量；现已将报告 payload 存储升级为长文本，并在应用启动时自动执行字段类型迁移。
  - 日志中心已改成与报告中心一致的独立页眉页面，系统页眉标题会按日志类型自适应显示，副标题固定为“日志中心”；页面内部原先的第一张大卡片已移除，仅保留筛选栏上方的状态信息与按钮。

## 2026-04-18 · 1.10.3 · 报告中心库存下钻与管理员账号修复

- 影响范围：报告中心、商品分布、管理员设置、账号同步
- 主要更新：
  - 报告中心中的库存变动明细、商品销售明细、审批通过工单、待审批 / 草稿均已改成固定高度卡片，数据较多时直接在卡片内部滚动，不再把整页高度继续撑长；同时解除库存变动明细、商品销售明细和工单明细的前后端条数截断。
  - 库存报告已从“门店库存分布”升级为“库存分布”，统计范围补上仓库、其他仓和维修点；库存数量右侧会同时显示本周期净流入 / 净流出变化，且每个地点都可点击进入查看该地点的具体库存变动明细。
  - 库存变动明细里的每一条记录现已支持点击后弹出二级操作菜单，可继续进入该商品的库存分布，或追溯到关联销售 / 工单记录。
  - 销售金额报告里的“销售员排名”已移除，避免与下方已有的“销售员排行榜”重复。
  - 修复商品分布二级菜单层级过低的问题，桌面端和移动端都已抬高基础层级，不再被父层商品分布弹窗遮挡。
  - 管理员设置弹窗内新增顶部即时提示，修改账号（手机号）和密码时会直接在弹窗内显示结果；同时复核 AQC 与账号中心的同步更新 / 重置密码接口，保持现有账户数据同步链路不变。

## 2026-04-18 · 1.10.2 · 报告中心页眉独立化与日志筛选收口

- 影响范围：报告中心、通知中心、统一日志中心、全局页眉
- 主要更新：
  - 报告中心已进一步独立为完整页面，页眉标题直接显示“报告中心”，原先页面内顶部介绍卡已移除，页面结构更紧凑。
  - 报告摘要卡已移除，避免与顶部汇总卡重复；报告测试推送默认不再自动勾选任何成员，保存报告设置后会自动关闭设置窗口。
  - 日志中心中的报告范围改为下拉选择“总报告 / 门店报告”，不再要求手动输入；开始日期与结束日期也已合并到同一组日期范围筛选中。
  - 页眉中的报告通知不再显示报告类型标签，同时对旧报告通知正文做了接口层兜底清理，避免历史文案继续露出。

## 2026-04-18 · 1.10.1 · 报告中心收口与门店报告开关

- 影响范围：报告中心、通知中心、店铺/仓库管理、管理员设置、统一日志中心
- 主要更新：
  - 报告中心继续收口：卡片内边距、模块指标卡、详情展开区和按钮位置已按站内卡片规范统一，默认所有模块收起，仅在卡片右下角展开详情，并继续兼容深色模式。
  - 报告设置入口已从管理员设置移到报告中心，只对管理员显示；侧边栏中的报告中心入口已移除，改为从店铺/仓库管理页工具栏进入。
  - 店铺/仓库管理的店铺编辑现已支持单店开启或关闭报告功能，并明确把 `AQC Flow` 排除在正常门店报告统计之外。
  - 页眉报告通知不再展示多余正文，通知列表仅保留当天的报告提醒；同时新增可配置的报告推送时间、通知清理时间与日报/周报历史保留天数，默认日报与周报保留 35 天、月报长期保留。
  - 日志中心的报告日志新增“报告范围”筛选，便于按总报告或门店维度快速回查历史。

## 2026-04-18 · 1.10.0 · 报告中心上线与移动端销售录入改版

- 影响范围：报告中心、通知中心、管理员设置、统一日志中心、销售录入、销售记录、移动端
- 主要更新：
  - 新增“报告中心”能力，支持日报、周报、月报按北京时间回顾上一完整周期生成，并在页眉通知中常驻推送，支持从通知直接进入查看详情或从日志中心回看历史报告。
  - 管理员设置页新增“报告设置”入口，可分别配置日报、周报、月报的启停、接收身份和额外接收成员，并支持直接发起报告测试推送；移动端会自动改为上滑面板样式。
  - 后端补上报告设置、报告日志与持久通知链路，报告会按身份与所属门店自动裁剪总报告或分门店报告，同时支持销售金额、销售商品、库存、销售员、目标、工单等模块的汇总与详情。
  - 调整移动端销售录入页按钮布局：摄像头识别条码与提交销售记录改为独占整行并提升高度，维修销售录入、查看销售记录、导入历史销售等按钮重新按移动端空间做了分排收口。
  - 修复移动端销售记录第一页搜索卡中的搜索框和按钮左右边距不一致问题，现在已与筛选面板及站内其他页面保持一致，并同步适配深色模式变量。

## 2026-04-17 · 1.9.45 · 移动端全局搜索自动聚焦与初始框体对齐

- 影响范围：全局搜索、移动端
- 主要更新：
  - 全局搜索输入框补上 `autofocus`、`inputmode="search"`、`enterkeyhint="search"` 等移动端搜索输入属性，并在打开后增加多轮聚焦重试，移动端呼出全局搜索时会自动进入光标并尽量直接拉起键盘。
  - 移动端聚焦时不再默认全选输入内容，而是把光标稳定放到输入末尾，保留更自然的原生搜索输入手感。
  - 全局搜索关闭态新增与展开态等宽的外层框体，四边留白保持一致；深色模式下的底色、边框和层级也一起收口。

## 2026-04-17 · 1.9.44 · 全局搜索承载层背景模糊强制关闭

- 影响范围：全局搜索
- 主要更新：
  - 为包含全局搜索的 `el-overlay / el-overlay-dialog` 承载层追加了强制透明和 `backdrop-filter: none` 覆盖。
  - 现在全局搜索承载层不会再把后面的页面内容做高斯模糊。
  - 搜索外层卡片和搜索框本体样式、功能与动效继续保留。

## 2026-04-17 · 1.9.43 · 全局搜索宿主层卡片与模糊彻底移除

- 影响范围：全局搜索
- 主要更新：
  - 移除了全局搜索仍然继承自通用对话框的宿主层卡片样式。
  - 去掉了宿主层的背景、边框、阴影和 `backdrop-filter`，只保留搜索外层卡片与搜索框本体样式。
  - 修复展开后多出一层卡片，以及动画结束后外围仍带模糊遮罩的问题。

## 2026-04-17 · 1.9.42 · 全局搜索外圈白雾与宽边框收窄

- 影响范围：全局搜索
- 主要更新：
  - 收窄了全局搜索关闭态的外层宽边框，让搜索框回到更贴近之前的紧凑尺寸。
  - 修复结果展开动画结束后，外层卡片突兀出现一圈白雾 / 白色模糊包边的问题。
  - 现在搜索框本体仍保留底色和边框，但外围不再出现额外的模糊遮罩。

## 2026-04-17 · 1.9.41 · 全局搜索卡片底色与开合动效恢复

- 影响范围：全局搜索
- 主要更新：
  - 回滚全局搜索的自定义浮层重做，恢复为原来的 `el-dialog` 开合结构。
  - 保留“无外围遮罩”的处理，但恢复全局搜索自身的深色卡片底、边框、阴影和开合动效。
  - 现在打开全局搜索时，可以继续看到页面背景，同时搜索层自身的视觉样式与之前一致。

## 2026-04-17 · 1.9.40 · 全局搜索入口点击冒泡修复

- 影响范围：全局搜索
- 主要更新：
  - 修复全局搜索按钮点击后被文档级关闭监听立即关掉的问题。
  - 现在点击页眉搜索按钮会稳定打开全局搜索浮层，不再出现“根本点不出来”的现象。

## 2026-04-17 · 1.9.39 · 全局搜索浮层重构与全局搜索商品分布菜单补齐

- 影响范围：全局搜索、商品分布、统一日志中心
- 主要更新：
  - 全局搜索已从 `el-dialog` 改成自定义固定浮层，不再生成任何外部遮罩或毛玻璃背景。
  - 全局搜索里的“库存分布”弹层现已补上点位条目点击后的二级菜单，桌面端与移动端都可继续进入调拨单或库存日志。
  - 全局搜索商品分布里的“库存日志”以及二级菜单跳转前，都会自动关闭当前弹层和菜单，不再残留原面板。

## 2026-04-17 · 1.9.38 · 全局搜索毛玻璃彻底去除与商品分布菜单收口

- 影响范围：全局搜索、商品分布、统一日志中心
- 主要更新：
  - 全局搜索已额外取消透明搜索对话框自身的毛玻璃滤镜，不再残留外部背景虚化效果。
  - 商品分布在移动端点击每个分布条目时，会先收起父层分布面板，再弹出“创建商品调拨单 / 查看日志”二级菜单，避免继续被父层影响而看不见。
  - 店铺/仓库管理里的商品分布“库存日志”入口现已补上自动关闭父层与子层菜单，跳转日志中心时不再残留原弹窗。

## 2026-04-17 · 1.9.37 · 全局搜索无遮罩恢复与商品分布二级菜单层级修复

- 影响范围：全局搜索、商品分布
- 主要更新：
  - 全局搜索已取消外部 modal 遮罩，不再对页面外层加背景虚化蒙层，交互恢复为更接近 macOS 聚焦搜索的无遮罩悬浮样式。
  - 商品分布里的二级菜单已单独抬高桌面端与移动端层级，修复点击分布条目后“创建商品调拨单 / 查看日志”菜单被父层压住而看不见的问题。
  - 同步收口了移动端底部上滑面板的基础层级分配，避免后续同类二级面板再被父级商品分布层遮挡。

## 2026-04-17 · 1.9.36 · 目标移动端改版与商品分布层级修复

- 影响范围：店铺目标、商品分布、全局搜索
- 主要更新：
  - 店铺目标页的月份详情在移动端已改成上滑菜单，编辑本月目标会在详情层之上继续弹出，不再被详情层遮挡。
  - 目标月历移动端卡片已重做为更紧凑的排布：完成比值改为更强调的胶囊数字，目标与实际放到同一排，进度条统一收到底部。
  - 本月目标现已在月历上方单独增加一张置顶卡片；桌面端展示更完整的阶段、奖励和型号信息，移动端保持与月历卡片一致的样式节奏，同时月历里仍保留本月卡片。
  - 目标型号取消件数设定，改为每销售一只就按该型号奖励金额累计一次奖励；详情和卡片中的型号奖励统计已同步切换到新口径。
  - 商品分布移动端统一改成无卡片的紧凑上滑面板，减少层层卡片堆叠；同时移除了之前过高的商品分布弹层层级覆盖，修复二级菜单 / 后续弹窗容易被压住的问题。

## 2026-04-17 · 1.9.35 · 目标阶段与型号目标真实保存修复

- 影响范围：店铺目标
- 主要更新：
  - 修复店铺目标保存接口把阶段目标和型号目标误当成空数组的问题。
  - 现在保存两阶段目标时，会正确写入 `130000` 和 `200000` 两个阶段，不再只剩一个最高金额阶段。
  - 现在保存型号目标时，会正确写入所选商品，不再出现“确认选择后保存成功但回到详情仍显示未配置型号目标”的问题。
  - 已按真实线上接口模拟武汉新佳丽 Casio 专卖店 `2026-04` 数据，验证两阶段目标与 `GMW-BZ5000RC-1PR` 型号目标均可正常落库与回读。

## 2026-04-17 · 1.9.34 · 目标阶段金额显示修正与目标保存链路补齐

- 影响范围：店铺目标、商品选择弹窗
- 主要更新：
  - 目标编辑区里“仅保留阶段目标，最高阶段金额会作为当月目标额展示”的提示已移除。
  - 目标卡片和详情中的目标金额改为显示“当前未达成阶段金额”；未达第一阶段时显示第一阶段金额，已达第一阶段未达第二阶段时显示第二阶段金额。
  - 修复阶段目标保存后只剩一条的问题，保存时现已直接按当前月份编辑草稿提交，不再把阶段配置在全量序列化过程中丢掉。
  - 目标型号选择现已按当前店铺库存查询商品，并将选择时的店铺库存数量带入目标件数。
  - 修复目标型号确认选择后保存不生效的问题，型号目标现在会随当前月份一起正确写入并在详情中回显。

## 2026-04-17 · 1.9.33 · 目标阶段编辑修复与商品选择清单补齐

- 影响范围：店铺目标、商品选择弹窗
- 主要更新：
  - 目标进度条的阶段节点已改成纯色小圆点，不再保留外圈轮廓，并重新校正到与进度条垂直居中。
  - “阶梯目标 / 新增阶梯”文案统一改成“阶段目标 / 新增阶段”。
  - 修复目标编辑里新增第二个阶段会覆盖第一个阶段的问题，阶段列表改为稳定键值渲染，可连续新增和独立编辑多个阶段。
  - 目标型号选择弹窗补上和工单一致的已选商品清单，支持像购物车一样查看、取消、清空并确认当前选择。
  - 修复商品目标保存链路，保存时会一并保留商品 ID、型号、品牌、系列和条码信息，避免保存后回读状态丢失或已选商品无法正确回显。

## 2026-04-17 · 1.9.32 · 目标日志统一接入与目标月历按月编辑改版

- 影响范围：店铺目标、统一日志中心、店铺/仓库管理
- 主要更新：
  - 店铺目标页的“目标日志”已改为进入统一日志中心，支持按店铺、月份、日期和关键词筛选目标日志。
  - 目标月历卡片改回纯白卡面，取消渐变背景；进度条改为浅金色，未达成节点改为深灰色、达成节点改为深金色，并把阶段目标数据统一放到进度条下方。
  - 编辑入口不再放在页面顶部，改成先打开月份详情，再在详情弹窗里编辑当前月份目标并直接保存。
  - 编辑目标时移除了单独的销售额目标输入，仅保留阶梯目标；最高阶梯金额会自动作为当月目标额展示和保存。
  - 型号目标改成“新增型号”模式，现可复用商品选择弹窗批量勾选商品，并为每个型号单独设置奖励；原型号组预设和统一组配置入口已从主流程移除。
  - 修复目标编辑后卡片已更新但保存后又恢复的问题，保存当前月份后会按最新服务端结果重新回填详情和月历卡片。

## 2026-04-17 · 1.9.31 · 店铺目标子页面上线与全局搜索库存分布交互修正

- 影响范围：页眉全局搜索、店铺/仓库管理、店铺目标
- 主要更新：
  - 全局搜索里的“库存分布”结果继续保留为当前页弹窗，但现在点击词条后会先关闭搜索层，再在当前页打开商品分布弹窗，不再保留搜索框遮挡页面。
  - 店铺/仓库管理顶部按钮文案改成“新增地点”，店铺编辑新增“目标功能”开关，只有开启目标的店铺才会在操作栏显示“目标”按钮。
  - 已为 `武汉新佳丽Casio专卖店`、`武商世贸Casio专柜`、`宜昌国贸Casio专卖店`、`武商梦时代Casio专卖店`、`十堰武商Casio专柜` 默认开启目标功能。
  - 新增店铺目标子页面，支持按年查看 12 个月目标月历、月份进度详情、店员贡献占比、型号目标完成情况和目标日志。
  - 管理员可进入目标编辑模式，对每个月的销售额目标、阶梯奖励和型号组目标进行编辑；型号组支持保存和删除预设，保存/退出编辑逻辑与排班页保持一致。

## 2026-04-17 · 1.9.30 · 全局搜索库存分布弹窗与入职时间口径切换

- 影响范围：页眉全局搜索、统计与账户
- 主要更新：
  - 全局搜索里的“库存分布”结果不再跳转到商品管理，而是在搜索框上层直接弹出商品分布弹窗，关闭后可以继续沿用原搜索关键词和结果。
  - 统计与账户里的“已加入 AQC”卡片现在支持点击切换显示口径，默认显示 `xx 天`，并可依次切换为 `xx 周余 xx 天`、`xx 月余 xx 天`、`xx 年余 xx 天`。
  - 深色模式下全局搜索分布弹窗层级和入职时间卡片的交互样式也一起收口，避免被搜索层压住或出现悬停跳色。

## 2026-04-17 · 1.9.29 · 店铺商品分布弹窗回归与对比预设删除样式收口

- 影响范围：店铺/仓库管理-商品、商品管理-对比
- 主要更新：
  - 店铺 / 仓库管理里的商品列表点击“分布”后，不再跳转到商品管理页面，而是在当前页直接打开商品分布弹窗。
  - 店铺页内的商品分布弹窗继续沿用了商品管理原有的展示样式，并保留调拨、查看日志等动作入口。
  - 商品管理对比模式里的预设删除按钮改成右上角小叉样式，视觉与排班卡片删除按钮保持一致。

## 2026-04-17 · 1.9.28 · 商品对比预设删除与店铺商品分布入口补齐

- 影响范围：商品管理-对比、店铺/仓库管理-商品
- 主要更新：
  - 商品管理的对比模式预设现在支持直接删除，保留已保存预设的同时也方便收掉旧方案。
  - 商品对比表格去掉了各店铺 / 仓库下的金额子列，保留库存和销量，横向信息更紧凑。
  - 商品管理列表分页新增 `10 条 / 页` 选项，在对比模式下查看宽表时更容易控制单页信息量。
  - 店铺 / 仓库管理里的商品列表新增“操作”列和“分布”按钮，可以直接跳到商品管理中的分布视图，继续使用同一套分布样式和功能。

## 2026-04-16 · 1.9.27 · 排班分组过滤与退出编辑回滚修复

- 影响范围：店铺排班
- 主要更新：
  - 排班编辑悬浮窗和班次弹窗现在只显示已开启排班功能的店铺，不再把未开启排班的店铺和人员混进分组列表。
  - 退出编辑时如果当前排班有未保存修改，确认退出后会回到上一次已保存的排班状态，不会再把本地临时改动留在页面上。
  - 进入编辑模式时会基于最新已加载的服务端排班快照启动，避免旧的本地临时状态残留。

## 2026-04-16 · 1.9.26 · 排班开关、管理员归店与当前店优先展示

- 影响范围：店铺/仓库管理、店铺排班
- 主要更新：
  - 店铺编辑新增“排班功能”开关，只有开启排班的店铺才会显示排班入口，也才允许进入排班页面。
  - 已为 `武汉新佳丽Casio专卖店`、`武商世贸Casio专柜`、`宜昌国贸Casio专卖店`、`武商梦时代Casio专卖店`、`十堰武商Casio专柜` 默认开启排班功能。
  - 排班编辑悬浮窗和班次弹窗里的店铺分组顺序改成“当前店铺优先”，不再按固定顺序把当前店排到后面。
  - 管理员如果绑定了所属店铺，也会作为可排班成员出现在对应店铺分组和排班弹窗中。
  - 店铺列表新增排班状态展示，会显示当前店铺是否开启排班以及排班成员数。
  - 排班分组卡片展开后不再保持黄色高亮，展开态颜色已和普通卡面统一，深色模式同步生效。

## 2026-04-16 · 1.9.25 · 排班跨店成员分组、搜索与同班冲突确认

- 影响范围：店铺排班、排班统计、排班日志
- 主要更新：
  - 排班编辑悬浮窗和班次弹窗支持按店铺分组查看当前店与其他店的销售员、店长，其他店默认折叠显示。
  - 两处人员选择区新增统一搜索框，可以直接搜索本店或非本店的销售员、店长。
  - 当其他店人员加入当前店排班后，统计卡会补上对应人员，并按该人员在所有店铺的当月排班数据计算统计。
  - 班次弹窗里的人员按钮整体收得更紧凑，减少留白，便于快速多选。
  - 保存排班时如果检测到同一人同一天同一班次在别店也有排班，会先弹出工单同款确认框，再决定是否继续保存。

## 2026-04-14 · 1.9.24 · 变动库存销售单库存提醒修正

- 影响范围：工单管理、销售类工单审批
- 主要更新：
  - 修复变动库存销售单中“销售店铺和发货店铺/仓库不一致”时，库存异常提醒误拿销售店铺库存做判断的问题。
  - 现在无论是编辑工单、提交审批还是审批通过前的二次确认，库存提醒都会统一按发货店铺/仓库库存判断。
  - 工单详情页回填的行内库存也已同步改成读取每一行的发货店铺/仓库库存，避免审批弹窗继续看到旧口径。

## 2026-04-14 · 1.9.23 · 销售追溯收口与工单折扣联动

- 影响范围：销售记录、工单管理
- 主要更新：
  - 销售记录通过搜索跳转或日志追溯跳转进入时，默认不再自动展开筛选面板，落地后会直接定位记录区域。
  - 商品进货单的工单明细表已把第一列商品名称固定常驻，横向滚动时不会再丢失商品识别信息。
  - 变动库存销售单的折扣列头新增“整单折扣”，输入折扣后会批量同步每一行折扣和实付金额；单行折扣编辑也会即时联动实付金额。
  - 工单明细的汇总行和底部总计已补充整体折扣计算，销售类工单的默认事由也改成不带日期的点位命名格式。

## 2026-04-14 · 1.9.22 · 全局搜索工单范围与日志追溯补齐

- 影响范围：页眉全局搜索、日志中心、订单管理、工单管理
- 主要更新：
  - 管理员在全局搜索里搜索工单时，现已按全部工单范围查询，不再只返回本人可见的“我的工单”口径。
  - 订单管理正式恢复为独立页面路由，保证全局搜索和日志追溯进入订单时不会再被错误重定向到销售记录。
  - 日志中心表格最后一列新增“操作”，当日志关联了工单、订单或销售订单号时，会显示“追溯”按钮并自动跳到对应页面带筛选。
  - 全局搜索专用蒙层和内容层的层级再次独立加强，固定高于现有卡片弹窗、二级菜单和通用对话框层级。

## 2026-04-14 · 1.9.21 · 全局搜索订单工单直达与层级修正

- 影响范围：页眉全局搜索、订单管理、工单管理
- 主要更新：
  - 全局搜索现在支持更自然地搜索订单号和工单号，点击结果后会直接进入对应页面，并自动带上相应筛选条件。
  - 订单搜索结果已改为进入订单管理页，不再错误跳到销售记录页。
  - 工单搜索结果进入工单管理页时会自动带入工单号关键词筛选，并继续保留工单详情高亮打开能力。
  - 全局搜索专用的蒙层和内容层级已从通用弹窗层级中独立抬高，固定高于其他卡片弹窗和二级菜单。

## 2026-04-14 · 1.9.20 · 销售单默认库存模式与分布日志直达

- 影响范围：工单管理、全局搜索、商品管理-商品分布、日志中心
- 主要更新：
  - 销售单现在默认开启“变动库存”模式，工单信息区新增“发货店铺/仓库”选择项，库存扣减继续只作用于发货点位。
  - 销售单明细里的“销售店铺”和“发货店铺/仓库”新增了和“销售员”一致的“全列”批量设置入口。
  - 全局搜索弹层层级已进一步抬高，固定高于各类弹窗和二级菜单，避免被后开的浮层压住。
  - 商品管理里的商品分布动作菜单新增“查看日志”，会直接跳到日志中心并自动筛选“当前店铺/仓库 + 当前商品”的库存日志。
  - 日志中心的商品库存日志 / 店铺库存日志接口已补上交叉筛选能力，支持同时按商品和店铺维度缩小范围。

## 2026-04-14 · 1.9.19 · 销售发货点位与历史库存修正

- 影响范围：销售记录编辑、销售库存扣减、商品进货单、变动库存销售单、历史库存修复脚本
- 主要更新：
  - 销售记录编辑弹窗里的“发货店铺”已改成“发货店铺/仓库”，编辑历史销售时可以继续保留或切换到仓库发货。
  - 复核线上后确认“销售店铺与发货店铺不一致时双扣库存”不是当前录入逻辑，而是 `2026-04-07` 的销售恢复脚本误写入了 9 条 `销售恢复扣减`；本次已新增修复脚本并回补这批错误扣减的库存。
  - 商品进货单里的系列字段已从只读改为支持输入联想，商品名称/型号继续保留自动填充与联想补全。
  - 变动库存销售单把商品名称列移动到第一列并固定显示，选择现有商品后应付金额和实付金额会按商品价格自动带入。
  - 销售恢复脚本已补强为默认只恢复销售记录，不再默认二次扣减库存；如确需同步库存，需要显式开启参数。

## 2026-04-09 · 1.9.18 · 变动库存销售单提交流程修正

- 影响范围：工单管理、变动库存销售单、销售金额联动
- 主要更新：
  - 修复变动库存销售单在已填写完整的情况下，提交审批仍被误判为“销售记录信息不完整”的问题。
  - 提交审批前的销售校验现在会区分“绑定原销售记录的销售单”和“变动库存销售单”，不会再错误要求后者先填写订单号。
  - 实付金额输入后现在会立即联动回算折扣，换入、换出和普通销售明细都已接入同样的金额同步逻辑。

## 2026-04-07 · 1.9.17 · 销售周期文案与群组弹窗收口

- 影响范围：主页销售统计、销售记录、工单管理-群组管理
- 主要更新：
  - 主页和销售记录里的销售额周期切换文案，已从 `1天 / 1周 / 1月` 调整为 `本日 / 本周 / 本月`。
  - 群组管理弹窗去掉了内部大卡片容器，改回和其他弹窗一致的纯内容布局，不再出现明显的嵌套卡片观感。
  - 群组列表项也同步收成更轻的列表块样式，保持信息层次清晰，同时避免弹窗里视觉过重。
  - 补充修正：重新校正了后端 `sales.py` 的线上发布路径，确保主页和销售记录读取到的周期文案已经实际生效。

## 2026-04-07 · 1.9.16 · 工单库存零值提醒补齐

- 影响范围：工单管理、库存异常二次确认
- 主要更新：
  - 工单保存草稿、提交审批和审批通过前的库存异常提醒，新增了“发货库存为 `0`”的提醒条件，并继续和“库存小于 `0`”共用同一套确认弹窗。
  - 审批通过入口也已接入同样的提醒逻辑，避免编辑页和审批页的库存风险提示口径不一致。
  - 商品调拨、销售退货、报损、变动库存销售等涉及发货库存的工单，在发货库存为 `0` 时也会先提醒再继续操作。

## 2026-04-07 · 1.9.15 · 图表轮询静默刷新

- 影响范围：主页销售统计、销售记录统计面板
- 主要更新：
  - 主页和销售记录里的销售图表在自动轮询刷新时不再重新播放入场动画，只会静默更新数据。
  - 首次进入页面时仍然保留原有图表动画，手动切换统计周期时也继续保留过渡动画。
  - 销售记录页联动的统计面板轮询刷新已同步改成静默模式，避免停留页面时反复重播图表。

## 2026-04-07 · 1.9.14 · 销售统计样式回滚

- 影响范围：主页销售统计、销售记录、销售日历统计头、销售额切换按钮
- 主要更新：
  - 把销售额与统计卡片中的金额显示回滚到你截图对应的旧版设计，恢复原来的字体、字号和数字观感。
  - 销售日历头部金额、主页销售额以及下方统计切换按钮里的金额显示统一回滚，不再使用新版字符拆分渲染。
  - 彻底取消翻牌式过渡动画，排除动效对金额排版与视觉观感的影响。

## 2026-04-07 · 1.9.13 · 销售额翻牌视觉回正

- 影响范围：主页销售统计、销售记录统计面板
- 主要更新：
  - 销售额翻牌组件改回完全继承上一版的字体、字号、字重和字距，不再额外改变原来的数字观感。
  - 销售额数字进入页面时会先从 `0` 开始，再翻牌到真实金额，保留动态感但不改变原有排版。
  - 冠军名称等非金额文字不再套用翻牌动画，避免统计卡片整体视觉变花。

## 2026-04-07 · 1.9.12 · 销售日历负数显示与静默刷新

- 影响范围：销售记录、销售日历、主页销售统计
- 主要更新：
  - 销售日历和日历详情现在可以正确显示退货带来的负数销售额，不再把负数统一抹成 `0`。
  - 销售记录页新增静默轮询刷新，会在停留页面时自动更新销售记录列表、销售日历和统计摘要，不需要手动刷新才能看到最新销售。
  - 主页与销售记录页的核心销售数字新增翻牌式过渡动画，销售额变化时会更自然地递增或递减显示。

## 2026-04-07 · 1.9.11 · 管理员可兼任维修工程师

- 影响范围：店铺/仓库管理、维修销售录入、维修销售记录编辑
- 主要更新：
  - 维修点中的“工程师”选择不再只允许 `aqc_engineer`，管理员账号现在也可以作为维修点工程师被分配与保存。
  - 维修销售录入页面中的工程师候选名单同步放开到“工程师或管理员”，管理员可直接作为维修人员录入维修销售。
  - 维修销售记录编辑弹窗中的工程师选择也同步放开管理员，避免历史记录编辑时再次被角色筛选拦住。

## 2026-04-07 · 1.9.10 · 李玲三月销售恢复与历史误删修正

- 影响范围：销售记录、销售日历、个人业绩、参考提成/业绩排名、历史销售数据恢复
- 主要更新：
  - 追查到“李玲 / 澳群线上Casio淘宝”三月销售缺失的根因不是前端筛选，而是 `2026-04-01` 清理异常销售记录时，把一批本应保留的线上店销售误删了。
  - 新增 TSV 恢复脚本，用备份文件把误删的 9 条 `2026-03` 线上销售记录安全写回生产库，并同步补回对应库存与库存日志，避免只补销售表却遗漏库存链路。
  - 线上复核后，“澳群线上Casio淘宝”三月销售已从 `1` 条恢复到 `10` 条，李玲三月个人销售已从 `2` 条恢复到 `11` 条，销售记录、销售日历和个人业绩口径重新一致。

## 2026-04-07 · 1.9.9 · 销售退货金额口径修正

- 影响范围：销售记录、销售日历、销售统计、参考提成/业绩排名
- 主要更新：
  - 把销售退货相关的金额与数量口径统一为同一套状态规则：原销售单 `returned` 始终保留原始正向金额，仅作为已退货标记展示；退货冲销单 `return_entry` 负责单独扣减金额与数量。
  - 销售记录列表、销售日历、销售汇总、个人业绩与排行等聚合链路全部改为按这套规则计算，避免原销售与退货冲销同时各扣一次，导致金额被双重扣减。
  - 复核了李玲 2026 年 3 月销售数据与现有退货记录，确认 3 月销售明细仍在库中，同时为历史退货数据增加了更稳妥的展示归一化，降低同类问题再次出现的风险。

## 2026-04-06 · 1.9.8 · 提成比例与排班权限细化

- 影响范围：统计与账户、店铺/仓库管理、排班日历、工单管理、主页排班卡片
- 主要更新：
  - 参考提成的“业绩时间范围”弹窗新增“提成比例”设置，默认 `2%`，可自行修改并按当前账号本地保存。
  - 店铺/仓库管理中的排班入口按账号所在店铺细化权限，非管理员账号不再看到非本人店铺的排班按钮；排班日历中高亮也改为当前登录账号，而不再默认突出店长。
  - 非管理员账号不再显示“定时开单”按钮，主页“明日排班类型”在无排班时改为仅显示“明日暂无排班”，并去掉空状态虚线框。

## 2026-04-05 · 1.9.7 · 移动端弹层层级回正

- 影响范围：工单编辑、销售记录、排班日历及其他移动端二级弹层
- 主要更新：
  - 统一上调 Element Plus 对话框、确认框、下拉菜单与自定义浮层的层级基线，避免移动端上滑面板继续压住二次确认框和二级弹窗。
  - 修复编辑工单时的二次确认菜单被上滑面板盖住的问题，同时恢复双击打开商品菜单和销售记录弹层的可见性。
  - 同步抬升销售日历详情浮层、排班人员卡片以及工单选品浮动摘要层，减少其它移动端页面出现相同遮挡的风险。

## 2026-04-05 · 1.9.6 · 扫码登录固定小档位稳定化

- 影响范围：移动端扫码登录弹窗
- 主要更新：
  - 扫码登录上滑面板在固定小档位模式下，关闭内容区滚动与拖拽缩放交互。
  - 固定档位面板打开后锁定当前视口高度，避免相机初始化或浏览器视口变化再次带动面板尺寸跳动。
  - 扫码登录相机区域按面板高度直接计算显示区域，尽量减少 HUD 和画面比例在加载前后发生变化。

## 2026-04-05 · 1.9.5 · 移动端页眉留白与全站安全区回正

- 影响范围：移动端页眉、全站移动端布局容器
- 主要更新：
  - 移动端页眉在去掉卡片框后，恢复上下左右留白，不再紧贴屏幕边缘。
  - 移动端全站页面的左右安全区恢复到外层主容器上，避免其他页面内容整体贴边。
  - 内容层不再额外统一叠加横向内边距，防止已有移动端内边距的页面被二次压缩。

## 2026-04-05 · 1.9.4 · 页眉去框修正与登录切换按钮统一

- 影响范围：移动端页眉、页眉设置扫码登录弹窗、SyMuse 账号中心登录页
- 主要更新：
  - 修正移动端页眉去框未真正生效的问题，改为在模板层直接移除卡片基类，只保留原有头部高度与宽度节奏。
  - 移动端页眉继续仅显示主标题，不显示副标题。
  - 页眉设置里的扫码登录上滑面板继续保持仅最小缩放档位，避免相机初始化后自动跳大。
  - `account.symuse.com` 登录页里的“扫码登录”和“用账号密码登录/注册”两个切换入口，统一为同一套强调按钮样式。

## 2026-04-05 · 1.9.3 · 移动端页眉与扫码登录视觉收口

- 影响范围：页眉设置、移动端扫码弹窗、SyMuse 账号中心登录页
- 主要更新：
  - 移动端页眉去掉卡片式边框与阴影，只保留主标题显示，副标题在移动端隐藏。
  - 页眉设置里的扫码登录上滑面板恢复默认第一档展开，并把扫码 HUD 引导框修正为稳定正方形，避免随相机窗口比例被拉伸。
  - `account.symuse.com` 二维码登录页取消二维码外层卡片框，深色模式下也不再反转二维码颜色。
  - 二维码页下方的“用账号密码登录/注册”切换入口改成更大的强调按钮，同时把桌面端登录卡片整体宽度收回到更接近原先的尺度。
  - 同版本补充修正：移动端页眉仅去掉卡片框，不再改动原有头部高度与宽度；扫码登录上滑面板改为只保留最小缩放档位，避免相机加载完成后自动跳大。

## 2026-04-05 · 1.9.2 · 扫码页极简化与登录首页切换收口

- 影响范围：页眉设置、移动端扫码弹窗、销售录入/商品管理扫码弹窗、SyMuse 账号中心登录页
- 主要更新：
  - 页眉设置里的“扫码登录桌面端”改为“扫码登录”，移动端打开后默认就是最大档位，并收成纯相机扫描框，不再保留说明文字、底部按钮、状态卡片和外层卡片边框。
  - 销售录入、商品管理里的摄像头条码识别框新增与扫码登录同风格的引导框，但改成横向长圆角矩形，更适合条码扫描场景，且不影响原有识别逻辑。
  - `account.symuse.com` 登录页改回默认优先展示二维码，只保留圆角二维码和下方切换按钮；点击“用账号密码登录/注册”后，切回原来的纯账号密码登录样式。
  - 账号中心二维码页同步去掉多余说明、状态卡片和刷新按钮，并补上二维码过期后的自动刷新逻辑；深色模式下二维码会自动反色显示。

## 2026-04-05 · 1.9.1 · 扫码登录联动与销售日历权限细化

- 影响范围：销售记录、移动端弹窗层级、AQC 页眉设置、SyMuse 账号中心登录页
- 主要更新：
  - 销售记录 > 销售日历 在“当日总览 -> 店铺 -> 人员 -> 当日商品销售明细”这一层，改为由日历接口直接附带人员商品销售明细，店长与销售员角色也能稳定看到商品级详情，不再只停留在人员销售额。
  - 移动端业绩排名里再打开“排名时间范围”时，后开的上滑面板会自动盖在前一个面板之上，修复二级面板被底层排名面板压住的问题。
  - `account.symuse.com` 桌面登录卡片新增二维码登录区，会自动生成短时二维码并轮询手机确认状态，确认后桌面端自动完成登录或单点回跳。
  - AQC 移动端页眉设置新增“扫码登录桌面端”入口，点击后直接调起摄像头扫码，仅识别桌面登录二维码，并通过现有账号绑定关系完成授权确认。
  - AQC 后端新增对 SyMuse 账号中心二维码会话的校验与确认代理，账号中心后端新增二维码会话创建、查询、确认和消费链路，同时限制二维码不能被其他账号重复抢占确认。

## 2026-04-05 · 1.9.0 · 服务器安全加固与系统基线收口

- 影响范围：服务器系统基线、SSH、FTP、MySQL、Nginx / PHP、宝塔环境、AQC-N 运维记录
- 主要更新：
  - 升级宿主机系统漏洞相关包：`python/python-libs`、`nss/nss-tools/nss-sysinit`、`bind-*`、`git/perl-Git`、`rsync`、`openssh-server`，并补装 `tcp_wrappers`、移除 `telnet`。
  - 收紧 SSH 基线：启用登录超时、空闲超时和更严格的 KEX / MAC / Cipher 组合；同时补上命令行自动超时退出。
  - 修正 `pure-ftpd` 的 `Umask` 为 `177:077`，把宿主机 MySQL 收口到 `127.0.0.1:3306`，并关闭外部 Ping。
  - 调整口令与权限基线：`PASS_MIN_DAYS` 改为 `7`，清理 sudo 中的 `NOPASSWD` 配置，收紧 `/boot/grub2/grub.cfg` 权限到 `600`。
  - 对扫描列出的 SUID / SGID 文件做去特权处理，并在 Nginx fastcgi 层补充隐藏 `X-Powered-By` 兜底，降低 PHP 版本暴露风险。
  - 新增 Docker 宿主机自动清理脚本与 `systemd timer`，每天定时清理未被容器使用的旧镜像、旧停止容器和旧构建缓存，避免发版后镜像持续堆积。

## 2026-04-05 · 1.8.2 · 管理分页、商品分布调拨捷径与销售日历三级详情

- 影响范围：管理员设置、商品管理、工单管理、销售记录
- 主要更新：
  - 管理员设置 > 人员列表表格新增分页，默认每页 `20` 条，并保留原有搜索与筛选结果口径。
  - 商品管理 > 商品分布 里点击任一点位，会出现快捷小菜单；当前已支持“创建商品调拨单”，进入工单页时会自动带入当前商品、调出点位，并优先使用当前账号所属店铺作为默认调入点位。
  - 销售记录 > 销售日历 去掉了标题区重复月份，只保留日历导航中的月份展示。
  - 销售日历详情新增第三级钻取：从“当日总览”进入店铺后，可继续点击人员查看她当天在该店铺卖了哪些商品，以及各自销售额、销量和订单数，整体沿用原销售详情样式。
  - 补修销售日历三级详情的明细请求地址，修复点击“店铺 -> 人员 -> 当日商品销售明细”时出现 `HTTP 404 / {"detail":"Not Found"}` 的问题。

## 2026-04-04 · 1.8.1 · 进货单新商品匹配加固

- 影响范围：工单管理、商品管理、销售录入、日志中心
- 主要更新：
  - 商品与进货单自动匹配规则改为“条码优先、商品名称签名兜底”，不再继续沿用商品编号的残余匹配逻辑。
  - 支持同名商品存在不同条码；审批进货单时若条码缺失且遇到“同名但不同条码”歧义，会直接拦截并要求补充条码或手动匹配，避免库存串货。
  - 商品管理新增/编辑同步增加“无条码同名商品”冲突校验，避免取消编号后继续堆出重复空条码商品。
  - 商品进货单保存草稿时取消未匹配新商品的二次确认，仅在“提交审批 / 同意审批”前继续保留确认弹窗。

## 2026-04-04 · 1.8.0 · 商品编号退役与进货单新商品恢复

- 影响范围：商品管理、店铺/仓库管理、工单管理、排班、销售录入、日志中心、全局搜索
- 主要更新：
  - 店铺/仓库管理 > 商品 默认库存筛选改为“库存不为0”，并补充“库存异常（小于0）”选项；排班日历改为周日首列、周六末列。
  - 商品编号设计正式退役：商品管理、店铺商品、工单选品、销售录入、全局搜索、日志筛选等高频页面不再展示和依赖商品编号，商品列表默认排序改为更新时间。
  - 商品与目录口径同步调整为不再依赖编号判定“可用商品”，避免无编号但已建档的新商品被系统误隐藏。
  - 恢复商品进货单对未匹配商品的自动建档能力，审批通过时会优先按条码复用已有商品，未命中时再安全创建新商品并入库。
  - 在进货单保存草稿、提交审批、同意审批三个关键动作前，新增“本工单包含未匹配的商品，是否确认创建新商品”的二次确认弹窗，减少误建档风险。

## 2026-04-04 · 1.7.33 · 日志入口收口与上滑面板灵敏度微调

- 影响范围：日志中心、商品管理、移动端弹窗
- 主要更新：
  - 日志记录页取消日志类型切换，仅保留通过各日志入口进入的当前日志类型，避免在统一页内再次切换日志来源。
  - 日志筛选区改用独立网格布局，修复桌面端字段卡片被拉满整行、内容宽度不自然的问题。
  - 商品管理 > 商品分布 的移动端摘要区移除“全部点位”显示，仅保留更有用的有货点位和库存金额。
  - 移动端上滑面板进一步降低缩放阻力，轻推即可更快切换档位，并修正最大档位下内容回拉时无法顺畅缩小面板的问题。
## 2026-04-04 · 1.7.35 · 上滑面板内容滚动分流与关闭动画收口

- 影响范围：移动端弹窗
- 主要更新：
  - 上滑面板把“面板缩放手势”和“内容滚动手势”重新分流，未进入真正内容滚动状态前，慢速上拉和下拉都会优先驱动面板本身，不再让内容区提前跳动抢手势。
  - 最大档位下只有内容已经滚起来时才交给原生滚动，因此内容区恢复了移动端原生惯性；从顶部回拉时则继续用于缩小面板。
  - 松手后的切档判定同时参考拖拽距离和速度，慢速拖拽到足够距离也能稳定切到大档或小档，不再容易回弹。
  - 下拉关闭时同步放缓了面板与遮罩的退场动画，关闭不再像硬切。
  - 针对“大档位下拉切回小档位”继续补调，额外提高下拉缩放阶段的灵敏度并缩短切档距离，做成更接近上拉切大档位的镜像手感。
## 2026-04-04 · 1.7.36 · 多模块旧弹窗切换为移动端上滑面板

- 影响范围：销售记录、工单管理、商品管理、店铺/仓库管理、统计与账户、管理员设置
- 主要更新：
  - 新增统一的响应式弹窗适配壳，桌面端继续沿用 `el-dialog`，移动端会自动切换为新的上滑面板样式。
  - 维修/销售记录编辑、工单新建编辑、定时开单、群组管理、工单查看、商品新增、商品属性编辑、店铺/仓库新增编辑、店铺商品、业绩排名、账户新增编辑等旧大弹窗全部接入新的移动端上滑面板。
  - 明确保留两个小弹窗原样：新建工单里的“大类选择”小弹窗不变，商品编辑里的“选择编辑菜单”小弹窗不变。
  - 各模块在移动端继续沿用原有表单和表格内容结构，同时复用新的上滑面板交互、深色模式和安全区适配，减少旧大弹窗在手机上的宽度浪费与操作割裂感。
  - 继续补齐两个遗漏弹窗：页眉设置里的“更新日志”大弹窗，以及商品管理 > 编辑 > 商品数量，现已一并切换为移动端上滑面板。

## 2026-04-04 · 1.7.34 · 上滑面板松手切档自适应与日志筛选动画对齐

- 影响范围：日志中心、移动端弹窗
- 主要更新：
  - 移动端上滑面板重新调整为更接近 iOS 的释放判定，拖动时更跟手，松手后会按位移和速度自适应切到展开、收起或关闭。
  - 修正最大档位内容区回拉时不容易缩回的问题，下拉到内容顶部后继续下拉可顺畅切回小档位，小档位继续下拉才关闭。
  - 日志页“搜索与筛选”区域补回出场动画，并调整到标题卡片和详情表格之间的统一进入顺序。
  - 日志页展开筛选面板改为复用销售记录筛选面板的样式结构，修复此前展开动画和桌面端字段宽度表现不一致的问题。

## 2026-04-04 · 1.7.32 · 日志页收口与上滑面板手势修正

- 影响范围：日志中心、移动端弹窗、商品管理、店铺管理、排班、订单
- 主要更新：
  - 日志页页眉文案改为“日志记录 / 记录标准化操作”，搜索与筛选默认收起，移动端卡片宽度与留白同步收口。
  - 商品库存、门店库存、排班日志、订单上传日志不再因为缺少上下文直接拦截，而是改为在日志页内提供商品、店铺、月份、订单等自适应筛选项。
  - 日志页继续沿用原日志接口和原始日志数据，仅调整入口和筛选壳层，避免影响日志本身。
  - 移动端上滑面板手势改为优先缩放到最大 / 最小档位，到最大前不允许内容滚动；向下回拉时会先把内容带回顶部，再继续缩小面板，最小档位继续下拉才关闭。

## 2026-04-04 · 1.7.31 · 统一日志中心与上滑面板交互补齐

- 影响范围：日志中心、商品管理、店铺管理、排班、订单、移动端弹窗
- 主要更新：
  - 移动端上滑面板改为整张卡片都可参与拖拽，同一次手势里可以在面板高度变化和内容滚动之间平滑衔接。
  - 面板拉到最大档位后继续上拉会直接带动内容滚动，回拉时会先把内容带回顶部，再继续缩小面板；到最小档位继续下拉即可关闭。
  - 商品管理 > 分布 的移动端面板只保留右上角关闭按钮，去掉底部关闭按钮，交互更接近 iOS 上滑卡片。
  - 新增统一日志中心页面，商品库存、门店库存、工单、排班、订单上传和更新日志全部接入同一页面，并统一使用搜索筛选区和返回按钮。
  - 后端补齐排班日志独立查询接口和订单上传日志聚合查询接口，旧日志数据结构保持不变，接入方式改为无损跳转。

## 2026-04-04 · 1.7.30 · 全局搜索移动端回车收口与商品分布上滑面板

- 影响范围：全局搜索、商品管理、移动端弹窗
- 主要更新：
  - 全局搜索结果分组顺序调整为“库存分布”优先、“商品”第二，更贴合库存排查场景。
  - 移动端全局搜索按回车后不再直接跳进结果，而是只收起键盘，改为点按条目后再进入对应页面。
  - 商品管理 > 分布 在移动端改为 iOS 风格的全宽上滑面板，默认半屏展开，支持继续上拉放大、下拉收起。
  - 新面板补齐深色模式、底部安全区、摘要卡与点位列表布局，移动端不再被桌面弹窗宽度和表格留白限制。

## 2026-04-03 · 1.7.29 · 桌面端表格操作列按按钮内容自适应宽度

- 影响范围：工单、商品、店铺/仓库、销售、账户、订单
- 主要更新：
  - 桌面端表格操作列新增统一宽度计算，不再按页面写死固定宽度。
  - 工单主列表会按当前页真实会出现的按钮组合动态收口列宽，减少右侧空白。
  - 商品管理、店铺/仓库管理、销售记录、账户管理、订单管理等常用页面同步切到同一套自适应逻辑。
  - 工单相关选择弹窗里的纯按钮型操作列也同步收口，桌面端表现更统一。

## 2026-04-03 · 1.7.28 · 工程师首页与销售入口切换到维修模式

- 影响范围：首页、销售录入、销售记录、路由
- 主要更新：
  - 工程师账号点击销售记录时会自动进入维修销售记录，点击录入销售时会自动进入维修销售录入。
  - 首页右侧操作按钮会从“扫码录入销售”切换成“录入维修销售”，并直接进入维修销售录入页。
  - 首页销售额卡片会改用维修销售数据统计，标题同步切换为“维修销售额”。
  - 路由守卫新增工程师兜底，旧链接或直接访问普通销售路由时也会自动跳到维修销售页面。

## 2026-04-03 · 1.7.27 · 工单日志筛选、动作列右对齐与历史更新日志分页

- 影响范围：工单、通用表格、更新日志
- 主要更新：
  - 工单日志弹窗新增独立搜索与筛选区，支持按关键词、范围、工单类型和日期区间查询。
  - 表格里的操作按钮统一靠右对齐，减少右侧无按钮时的空白感。
  - 历史更新日志页新增分页，并改为按大版本号和单个范围标签筛选。
  - 根据每天开发日志补齐了 2026-03-17 到 2026-04-01 的历史版本记录。

## 2026-04-03 · 1.7.26 · 库存日志总库存口径补齐与历史商品总数展示

- 影响范围：商品管理、店铺/仓库管理、库存日志
- 主要更新：
  - 商品管理 > 分布 > 库存日志新增“商品总数”列，展示该商品在对应日志变更后的全店铺总库存。
  - 商品维度库存日志卡片主指标改为“总库存”，日志条数下沉为弱化信息。
  - 店铺/仓库管理 > 商品 > 库存日志新增“商品总数”列，展示该店铺在对应日志变更后的整体库存数量。
  - 前后端统一补齐 `currentQuantityTotal` 与 `totalQuantityAfter` 口径。

## 2026-04-03 · 1.7.25 · 静态资源发布根治、工单收货店铺与排班统计口径收口

- 影响范围：发布链路、工单、排班、销售录入、管理员设置
- 主要更新：
  - 服务器外层 Nginx 移除对 `/assets/` 的持久化目录旁路，静态资源统一回到前端容器自身提供。
  - 新建工单中“日期”移动到最后一栏；销售退货单和销售换货单（换入）新增“收货店铺/仓库”。
  - 排班日志改为仅保留最近三个月，排班统计改为按员工整月跨店铺汇总。
  - 销售录入实收金额改成灰色默认金额占位，管理员设置编辑弹窗补齐移动端适配。

## 2026-04-02 · 1.7.24 · 维修点、工程师与维修销售全链路上线

- 影响范围：店铺/仓库管理、账户身份、销售录入、销售记录、排班统计
- 主要更新：
  - 店铺/仓库管理新增“维修点”类型。
  - 账户身份新增“工程师”，点击销售记录时默认进入维修销售记录页。
  - 销售录入新增维修销售录入，销售记录新增独立维修销售记录页。
  - 店铺排班统计新增“连班数”。

## 2026-04-02 · 1.7.23 · 排班移动端收口与未保存离开确认

- 影响范围：排班、移动端、交互
- 主要更新：
  - 移动端排班编辑不再显示桌面专用的店铺人员悬浮窗。
  - 排班统计卡整体更紧凑，减少留白。
  - 排班页新增未保存离开确认，覆盖退出编辑、切换页面与刷新场景。

## 2026-04-02 · 1.7.22 · 排班退出确认与保存 422 修复

- 影响范围：排班、保存链路
- 主要更新：
  - 修复排班保存时 `confirmIncomplete` 被误传点击事件对象导致的 `HTTP 422`。
  - 保存请求改为固定提交有效布尔值。
  - 退出编辑改为不保存退出，并补上二次确认。

## 2026-04-02 · 1.7.21 · 排班保存放开、统计下移与删除交互细化

- 影响范围：排班、首页、交互
- 主要更新：
  - 排班未排满时也可以随时保存。
  - 排班统计区下移到日历下方，并补齐保存/退出按钮。
  - 已排班卡片支持右上角快捷删除，拖拽删除命中区更直观。

## 2026-04-02 · 1.7.20 · 排班交互重做、更新日志历史页与整周期预设统一

- 影响范围：排班、首页、更新日志、时间范围
- 主要更新：
  - 店铺排班页支持点击班次选人、桌面端拖拽排班和日志弹窗。
  - 首页排班卡去掉嵌套卡片，点击后可直达本店铺排班页。
  - 设置中的更新日志拆成最近三条加历史页。
  - 本周、本月、本年统一改为完整自然周期。

## 2026-04-02 · 1.7.19 · 店铺排班上线、首页排班卡与通知入口整合

- 影响范围：排班、首页、设置
- 主要更新：
  - 店铺管理新增独立排班页和排班日志。
  - 首页右侧改为排班摘要卡。
  - 通知模块迁移到页眉设置菜单，当前账号并入退出卡片。

## 2026-04-02 · 1.7.18 · 接口错误提示优化与销售后端兜底收口

- 影响范围：请求层、销售后端
- 主要更新：
  - 前端统一优化非 JSON 错误提示。
  - 销售后端把销售人匹配与店铺反查逻辑收成共用 helper。

## 2026-04-02 · 1.7.17 · 统计与账户解析失败修复与版本号纯大版本化

- 影响范围：统计与账户、设置
- 主要更新：
  - 修复统计与账户页的店铺排行接口异常。
  - 系统版本统一改为只显示大版本号。

## 2026-04-02 · 1.7.16 · 版本号规则切换与更新日志入口收口

- 影响范围：设置、版本规则
- 主要更新：
  - 基础版本号切换为 `1.7.16` 并按末位递增维护。
  - 设置中的系统版本卡改为卡片底部轻量入口查看更新日志。

## 2026-04-02 · 1.7.15 · 销售记录推荐筛选与版本更新日志入口

- 影响范围：销售记录、设置
- 主要更新：
  - 销售记录筛选升级为推荐筛选，常驻 8 个时间周期按钮。
  - 推荐按钮与手动日期范围共享同一套筛选状态。
  - 设置中的系统版本卡开始支持直接打开更新日志弹窗。

## 2026-04-01 · 1.6.1 · 统计与账户重构、账户设置下沉与日期面板中文化

- 影响范围：统计与账户、店铺管理、全局日期
- 主要更新：
  - 账户页重做为统计主页，新增加入天数、参考提成、个人排名等能力。
  - 原账户资料 / 密码 / 会话能力下沉为二级“账户设置”页面。
  - 店铺/仓库商品弹窗新增库存金额汇总，管理员账户编辑支持入职时间。
  - 全局日期面板统一切到中文月份与星期显示。

## 2026-03-31 · 1.5.1 · 工单销售员联动、商品销量链路与销售日历跳转

- 影响范围：工单、商品管理、销售记录
- 主要更新：
  - 工单销售类明细支持按所选销售店铺联动销售员。
  - 商品管理新增销量列、时间范围销量和对比模式销量排序。
  - 销售日历支持年份和月份快速跳转，点位列表顺序统一按 ID。

## 2026-03-30 · 1.4.1 · 销售店铺与发货店铺拆分、销售换货与库存日志移动端收口

- 影响范围：销售录入、工单、库存日志、群组
- 主要更新：
  - 销售录入、销售记录和工单拆分销售店铺与发货店铺。
  - 销售换货单上线，换入与换出明细分表处理。
  - 库存日志弹窗补齐移动端适配，共享群组支持默认群组。

## 2026-04-21 · 1.10.7 · 全局搜索输入修复、工单弹层收口与千条单据压测

- 影响范围：全局搜索、通知中心、工单、数据库容器、性能压测
- 主要更新：
  - 桌面端全局搜索移除了会反复 `focus/select` 的输入重试，快速输入时不再吞字或把前几个字母反复替换掉。
  - 页眉通知菜单改为基于稳定锚点和菜单容器做定位与点击判断，点击与悬停都可以正常呼出。
  - 工单编辑和详情弹层撤回了额外抬高的 `z-index`，负责人、共享群组、店铺等下拉菜单与确认框重新回到正常层级。
  - 工单列表和看板改成通过聚合查询计算明细条数、总数量和总金额，不再为大工单把所有明细整单拉到列表页。
  - 复核了 `aqc-n-db` 的 MySQL 低内存配置，并完成 `1000` 条商品进货单的事务级模拟压测；当前无需解除数据库容器内存限制。

## 2026-04-21 · 1.10.6 · 工单调拨链路修复、通知中心独立与大单据性能收口

- 影响范围：工单、全局搜索、报告中心、通知中心、销售记录、移动端
- 主要更新：
  - 工单元数据里的店铺成员改为按成员真实所属店铺计算，多店铺销售员在销售换货与商品调拨场景可正常选中。
  - 商品分布进入调拨单后会在预填完成时主动刷新库存，避免销售员、店长、工程师从商品分布进入时发货库存被旧数据锁成 `0`。
  - 大工单查看不再对每一行商品重复强制拉取库存，工单明细同步改成就地更新，降低 150+ 行单据的卡顿和重复数据风险。
  - 销售记录编辑现已限制为管理员，销售员、店长、工程师在前端和后端都会被禁止编辑既有销售记录。
  - 页眉通知已从设置菜单独立成按钮，补上通知类型、时间和详情；报告中心商品分布改为复用现有商品分布样式 / 动作，移动端继续使用上滑面板。

## 2026-03-29 · 1.3.1 · 商品负库存筛选修正与销售记录筛选布局统一

- 影响范围：商品管理、销售记录
- 主要更新：
  - 修复商品管理“库存异常（小于 0）”的漏出问题。
  - 销售记录页搜索与筛选结构统一为顶部搜索 + 筛选卡布局。

## 2026-03-27 · 1.2.1 · 商品库存筛选扩展、工单群组共享与通知联动

- 影响范围：商品管理、工单、通知、首页
- 主要更新：
  - 商品筛选补上库存为 0 与库存异常口径。
  - 工单群组共享接通邀请通知与共享草稿权限。
  - 首页全局搜索与通知区交互做了动效收口。

## 2026-03-26 · 1.1.1 · 工单仓库店铺选项修复与数量列前置

- 影响范围：工单、打印
- 主要更新：
  - 修复非调拨工单只能选仓库的问题。
  - 工单编辑、详情和打印里的数量列统一前置，并补上单位列。

## 2026-03-25 · 1.0.1 · 工单品牌系列联想补全

- 影响范围：工单、商品元数据
- 主要更新：
  - 工单明细中的品牌和系列输入改成自动联想。
  - 直接复用商品管理已有的品牌和系列元数据。

## 2026-03-24 · 0.9.1 · 销售导入门店标准化与别名纠偏

- 影响范围：销售导入、店铺映射
- 主要更新：
  - 新增统一的门店标准化映射与大小写无关查找 key。
  - 销售导入逻辑改为复用标准化门店名，解决旧系统门店别名问题。

## 2026-03-23 · 0.8.1 · 历史销售导入入口与当日销售导入

- 影响范围：销售导入、历史数据
- 主要更新：
  - 续接前一日导入策略，解析 3 月 23 日销售表。
  - 历史销售导入入口开始接入日常补录流程。

## 2026-03-22 · 0.7.1 · 销售表解析与线上 dry-run 严格校验

- 影响范围：销售导入、数据校验
- 主要更新：
  - 将 Excel 销售表结构化提取为 JSON。
  - 在服务器容器内执行只校验不写入的 dry-run，逐项检查商品、门店和重复订单。

## 2026-03-20 · 0.6.1 · 销售图表稳定性、主题切换与销售记录筛选重做

- 影响范围：销售汇总、主题、销售记录
- 主要更新：
  - 修复销售额图表切换时间段时的显示稳定性问题。
  - 统一主题切换和品牌图标链路。
  - 重做销售记录筛选区。

## 2026-03-19 · 0.5.1 · 统一登录换票 500 修复与账号中心联调恢复

- 影响范围：认证、账号中心
- 主要更新：
  - 定位并修复 `auth/symuse/exchange` 的线上 `500`。
  - 恢复 AQC-N 与 `account.symuse.com` 的换票登录链路。

## 2026-03-18 · 0.4.1 · 订单与商品迁移、账号系统收口与前端风格重构

- 影响范围：订单、商品、账户、前端风格
- 主要更新：
  - 梳理订单模块缺失、商品字段迁移不全和账号管理割裂等关键问题。
  - 新增账号子集接入接口，开始收口到 `account.symuse.com` 的 AQC 子集。
  - 前端视觉方向统一为黑白灰主色加金色强调。

## 2026-03-17 · 0.3.1 · 后端认证增强、会话管理与前端基线盘点

- 影响范围：认证、会话、前端重构
- 主要更新：
  - 为 OAuth 回调补上 state 校验、过期控制和一次性消费能力。
  - 开始规划登录态会话管理接口。
  - 盘点旧接口与前端重构方向，为后续大规模迁移打基础。
