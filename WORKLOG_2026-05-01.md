# 2026-05-01 开发日志

## 1.10.38 · 系统选择框与首页设置持久化

### 排查记录

- 复查首页 `DashboardView.vue` 的销售额设置保存链路，确认偏好已写入 `localStorage`，但读取发生在 `onMounted` 内，晚于 `SalesSummaryPanel` 首次按默认值初始化。
- 复查全局 `ElSelect` 覆盖层，确认工单管理页需要继续使用 Element Plus 原选择框，录入销售页需要走系统选择框。

### 今日完成

- 将首页销售额偏好读取前移到组件 setup 阶段，确保默认时间范围、默认点位、销售额类目和默认对比方式在图表首次渲染前生效。
- 新增系统选择框兼容层，保留原选择框外观、绑定值、清空、禁用、变更事件等常用能力。
- 工单页面选择框回退 Element Plus 原调用菜单，录入销售页面选择框改为系统选择框。
- 将前端版本号从 `1.10.37` 更新为 `1.10.38`。
- 同步维护 `UPDATELOG.md` 与 `client/public/update-log.json`。

### 本地校验

- `npm --prefix client run build`：通过，生成 `dist/version.json` 为 `1.10.38`。
- `git diff --check`：通过。

### 部署记录

- 已将 `client/dist/` 部署到 `1.14.197.141:/www/wwwroot/aqc.symuse.com/`，部署前静态目录备份保存在 `/root/aqc-deploy-backups/1.10.38-20260501160938/webroot-before.tgz`。
- 已使用同一份构建产物覆盖运行中的 `aqc-n-web:/usr/share/nginx/html/` 静态资源，并重载 nginx。
- 线上校验结果：
  - `https://aqc.symuse.com/version.json` 返回 `1.10.38`。
  - `https://aqc.symuse.com/update-log.json` 首条为 `1.10.38 · 系统选择框与首页设置持久化`。
  - `https://aqc.symuse.com/api/health` 返回 `{"success":true,"message":"ok"}`。
  - `aqc-n-web`、`aqc-n-api`、`aqc-n-db` 均为 `Up`，数据库为 `healthy`。

## 1.10.37 · 首页销售额图表范围与设置

### 排查记录

- 复查 `DashboardView.vue` 与 `SalesSummaryPanel.vue`，确认首页销售额卡片复用了销售记录页的图表组件，需要用插槽和首页专用开关收口，避免影响销售记录页。
- 复查 `sales.py` 中 `/sales/summary` 的周期窗口与上一周期计算逻辑，确认原有默认行为是完整上周期总额对比。
- 复查店铺选项接口与 `utils/shops.js`，确认可通过 `shopType === 0` 只暴露店铺点位，并复用既有店铺缩写规则。
- 本地预览阶段发现临时 mock API 将不同周期的 `sales` 写成同一个值，已确认这只是预览数据问题，真实前后端周期请求和汇总链路正常。

### 今日完成

- 首页销售额卡片标题恢复为“销售额”，副标题调整为“点位 · 时间”格式，例如“总 · 本月”“梦时代 · 本月”。
- 点击“销售额”标题改为呼出系统选择框切换点位，选择项使用店铺全称，展示时使用缩写；管理员可选总销售额，单店账号默认自身店铺。
- 点击右侧销售额金额改为打开销售额设置面板，移动端走上滑菜单，桌面端走弹窗。
- 销售额设置支持切换：
  - 门店销售额 / 维修中心销售额。
  - 默认显示时间范围：本日、本周、本月、年累计。
  - 默认数据对比：环比总额、环比同期、同比。
  - 默认显示点位。
- 设置返回时如存在未保存更改，会使用统一确认框二次确认；保存后会持久化当前账号的首页偏好并刷新图表。
- 首页图表 Y 轴与对比金额改为 `k / w` 简写；首页图表、工单卡片内边距与排班卡片保持一致。
- 后端 `/sales/summary` 新增 `compare_mode` 参数，补齐环比同期与同比对比窗口。
- 将前端版本号从 `1.10.36` 更新为 `1.10.37`。
- 同步维护 `UPDATELOG.md` 与 `client/public/update-log.json`。

### 本地校验

- `npm --prefix client run build`：通过。
- `python3 -m py_compile backend/fastapi/app/routers/sales.py`：通过。
- `git diff --check`：通过。
- 使用本地 Vite + mock API 预览首页：
  - 副标题显示为“总 · 本月”，切换点位后显示店铺缩写。
  - 点击“销售额”标题可切换点位。
  - 点击右侧金额可打开销售额设置。
  - 本日、本周、本月、年累计切换会发起不同 `period` 的销售摘要请求并更新金额。

### 部署记录

- 已将 `client/dist/` 部署到 `1.14.197.141:/www/wwwroot/aqc.symuse.com/`，部署前静态目录备份保存在 `/root/aqc-deploy-backups/1.10.37-20260501152646/webroot-before.tgz`。
- 已使用同一份构建产物覆盖运行中的 `aqc-n-web:/usr/share/nginx/html/` 静态资源。
- 已将后端 `sales.py` 覆盖到运行中的 `aqc-n-api:/app/app/routers/sales.py`，并在容器内通过 `python -m py_compile` 后重启 API。
- 已固化当前容器镜像：
  - `aqcsymusecom-api:latest` -> `sha256:650fddd0e937ea58acd22b11c5183133918d8bed0d7b84c9726a4a8da899eb91`
  - `aqcsymusecom-web:latest` -> `sha256:2736aabf8c2c06d7b5b254535ad19d3696c689d2691cc792a6be1d3ba7b12e5d`
- 线上校验结果：
  - `https://aqc.symuse.com/version.json` 返回 `1.10.37`。
  - `https://aqc.symuse.com/update-log.json` 首条为 `1.10.37 · 首页销售额图表范围与设置`。
  - `https://aqc.symuse.com/api/health` 返回 `{"success":true,"message":"ok"}`。
  - `aqc-n-web`、`aqc-n-api`、`aqc-n-db` 均为 `Up`，数据库为 `healthy`。
