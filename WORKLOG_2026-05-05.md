# 2026-05-05 开发日志

## 1.11.5 · 全局搜索商品信息密度微调

- 全局搜索上限从 `5` 统一调整为 `7`，商品、订单、工单、店铺等分组查询与展示上限保持一致。
- 商品结果标题改为仅显示商品型号；库存角标保持 `库存 N` 的原有样式和位置。
- 商品结果副标题新增两行结构：第一行展示售价与总销量，第二行展示品牌、系列和最近销售时间。
- 商品合并逻辑会从销售记录结果中提取 `soldAt`，同商品多条命中时保留最近销售时间。
- 移动端工单列表卡片标题单独降到 16px，并保留现有文本颜色、卡片背景和深色模式变量。

## 验证

- 已执行 `npm run build`。
- 已执行 `python3 -m compileall backend/fastapi/app`。
- 已执行 `git diff --check`。

## 1.11.4 · 全局搜索商品动作与销售预填

- 全局搜索结果把库存分布、商品详情和销售记录三类商品相关命中合并为单一“商品”分组，并按商品 ID 去重。
- 新增商品结果二级菜单，复用工单大类卡片样式；桌面端走 `ResponsiveDialog` 弹窗，移动端走同组件上滑菜单。
- 二级菜单包含库存分布、创建订单、商品详情和商品销售记录；创建订单排在库存分布之后。
- 录入销售新增 `prefill_goods_id` 路由预填，自动拉取商品详情并执行原有 `selectGoods` 逻辑；移动端预填后直接进入信息步骤。
- 销售记录新增前后端 `goods_id` 筛选参数，商品销售记录入口能按商品精确过滤，统计与日历查询同步带入该条件。
- 移动端录入销售定位页按钮从垂直居中改为垂直居上，沿用现有主题变量，深色模式不新增独立配色。

## 验证

- 已执行 `npm run build`。
- 已执行 `python3 -m compileall backend/fastapi/app`。

## 1.11.3 · 移动端工单链路与销售记录分布修复

- 调整工单管理移动端新建流程，主页进入新建工单时回到第一步“选择工单大类”，避免直接跳到工单类型。
- 重做工单明细移动端刷新链路，搜索型号或录入订单确认后会立即刷新卡片列表，不再依赖返回上一页后重新渲染。
- 工单明细查看/编辑上滑菜单统一使用标准 `MobileBottomSheet` 大档位，去掉半屏高度限制。
- 本地测试环境补充完整工单详情与状态流转数据，覆盖草稿、待审批、已通过、已驳回和销售换货待审批，支持查看、编辑、审批、转草稿、删除和保存。
- 销售记录商品分布改为全局商品分布入口，并向全局弹窗传入当前销售记录的商品兜底信息，接口失败时仍展示统一样式面板。
- 复查移动端深色模式相关样式，继续使用 `var(--bg-elevated)`、`var(--field-bg)`、`var(--border-light)` 等现有主题变量。

## 验证

- 已执行 `npm run build`。
- 已执行 `git diff --check`。
- 已确认本地 Vite 服务 `/sales/records` 与 `/work-orders` 可访问。
