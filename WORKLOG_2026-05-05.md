# 2026-05-05 开发日志

## 1.11.3 · 移动端工单链路与销售记录分布修复

- 调整工单管理移动端新建流程，主页进入新建工单时回到第一步“选择工单大类”，避免直接跳到工单类型。
- 重做工单明细移动端刷新链路，搜索型号或录入订单确认后会立即刷新卡片列表，不再依赖返回上一页后重新渲染。
- 工单明细查看/编辑上滑菜单统一使用标准 `MobileBottomSheet` 大档位，去掉半屏高度限制。
- 本地测试环境补充完整工单详情与状态流转数据，覆盖草稿、待审批、已通过、已驳回和销售换货待审批，支持查看、编辑、审批、转草稿、删除和保存。
- 销售记录商品分布改为全局商品分布入口，并向全局弹窗传入当前销售记录的商品兜底信息，接口失败时仍展示统一样式面板。
- 复查移动端深色模式相关样式，继续使用 `var(--bg-elevated)`、`var(--field-bg)`、`var(--border-light)` 等现有主题变量。

## 验证

- 已执行 `npm run build`。
- 已执行 `git diff --check`。
- 已确认本地 Vite 服务 `/sales/records` 与 `/work-orders` 可访问。
