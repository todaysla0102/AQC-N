# 2026-05-07 开发日志

## 1.11.7 · 移动端销售录入图片与底部确认

- 先检查了移动端录入销售、移动端工单、商品图片字段、上传资源代理、版本日志和既有开发日志；确认商品接口已提供 `coverImage`，移动端工单确认区使用底部 sticky 常驻样式。
- 移动端录入销售的信息步骤新增商品图展示：选中商品存在 `coverImage` 或图片列表兜底首图时，使用全局 `apiAssetUrl()` 解析后展示在商品信息区。
- 无图商品不显示额外图片占位，不影响扫码、搜索型号、预填商品、数量/金额联动和销售提交链路。
- “确认录入销售”按钮改为底部 sticky 常驻，并加上安全区 padding 与渐变底色，和移动端工单确认按钮的底部常驻交互保持一致。
- 本轮只调整移动端销售录入展示层和版本日志，未改销售提交、库存扣减、商品数据结构和工单业务逻辑；新增样式继续使用现有主题变量，适配深色模式。

## 验证

- 已执行 `npm run build`。
- 已执行本地移动端可视化验收：390px 视口分别检查亮色与深色模式，商品图片可见，图片资源路径正确解析为 `/uploads/...`，确认按钮在滚动前后保持底部常驻。
- Browser 插件初始化超时，已改用临时 Playwright + 系统 Chrome 验证；控制台仅出现既有的 `ElSelect` 重复注册和 `MobileBottomSheet` 透传属性 Vue warning，未发现本轮销售录入相关错误。
- 验收截图保存于 `/tmp/aqc-mobile-sales-light.png` 与 `/tmp/aqc-mobile-sales-dark.png`。
- 复查截图后补充紧凑选择框省略样式，避免销售店铺/发货店铺长简称贴近清空图标。
- 已部署到 `1.14.197.141:/www/wwwroot/aqc.symuse.com/`，本轮只替换静态前端产物，保留远端 `.user.ini` 和可能存在的 `uploads/` 目录。
- 部署前静态目录备份保存在 `/root/aqc-deploy-backups/1.11.7-20260507211011/webroot-before.tgz`。
- 已检查远端 `aqc-n-web`、`aqc-n-api`、`aqc-n-db` 容器均正常运行；`https://aqc.symuse.com/version.json` 返回 `1.11.7`，远端 `/api/health` 返回 ok。
