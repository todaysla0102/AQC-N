# 2026-05-08 开发日志

## 1.11.13 · 修复新建商品类工单弹窗

- 复查新建工单大类选择、桌面编辑弹窗、移动端工单流程和批量调拨模式的状态切换。
- 修复桌面端点击“商品类工单”后编辑弹窗可能不显示的问题：从桌面大类弹窗进入时强制走桌面编辑弹窗流程，不再受移动端流程状态影响。
- 调整 `openCreateDialogByCategory` 为异步打开：先重置表单并关闭大类弹窗，再等待下一帧打开编辑弹窗，避免同一轮状态更新造成弹窗内容未挂载。

## 1.11.13 验证

- 已完成 Python 编译检查：`backend/fastapi/app` 通过；生产容器继续使用 `PYTHONPYCACHEPREFIX=/tmp/aqc-pycache` 完成容器内编译检查。
- 已完成前端生产构建：`client/dist/version.json` 输出 `1.11.13`。
- 已完成代码检查：`git diff --check` 通过；构建产物中继续确认无 `apiOptimizedAssetUrl` 与 `/goods/images/optimized` 引用。
- 已部署到服务器 `1.14.197.141`，线上 `https://aqc.symuse.com/version.json` 为 `1.11.13`，`/api/health` 返回正常。
- 已确认线上入口引用 `index-BiUjyGcd.js`，且产物中包含批量调拨“确认生成”按钮文案。
- 未进行前端模拟操作，按需求仅做代码级检查、编译、构建与线上接口/静态产物校验。
- 服务器备份：`/root/aqc-deploy-backups/1.11.13-20260508222916/webroot-no-uploads.tgz`、`/root/aqc-deploy-backups/1.11.13-20260508222916/api-current.tgz`、`/root/aqc-deploy-backups/1.11.13-20260508222916/aqc-n-web-nginx.conf.bak`。

## 1.11.12 · 取消图片优化并修复批量调拨点位选择

- 按最新反馈取消商品图片按需压缩读取功能，前端图片引用恢复使用原有 `apiAssetUrl` 与 `/uploads/...` 静态地址。
- 后端撤回 `/api/goods/images/optimized` 接口、Pillow 依赖和优化缓存逻辑，避免所有商品图被导向新接口后无法加载。
- 修复批量商品调拨单第二步选项不显示的问题：将收货点位选项与已选点位 computed 恢复到组件全局作用域，供模板和明细表共同使用。
- 批量调拨继续保留发货点位过滤、收货点位点击排序、后续明细表按选择顺序生成列的逻辑。

## 1.11.12 验证

- 已完成 Python 编译检查：`backend/fastapi/app` 通过；生产容器因 `/app` 只读，使用 `PYTHONPYCACHEPREFIX=/tmp/aqc-pycache` 完成容器内编译检查。
- 已完成前端生产构建：`client/dist/version.json` 输出 `1.11.12`。
- 已完成代码搜索检查：前后端产物中已无 `apiOptimizedAssetUrl`、`/goods/images/optimized`、`Pillow` 图片优化链路引用。
- 已部署到服务器 `1.14.197.141`，线上 `https://aqc.symuse.com/version.json` 为 `1.11.12`，`/api/health` 返回正常。
- 已验证线上静态上传图片 `/uploads/...` 可直接访问，返回 `200 image/png`；线上入口引用的新前端 JS 中不再包含图片优化接口路径。
- 未进行前端模拟操作，按需求仅做代码级检查、编译、构建与线上接口/静态资源校验。
- 服务器备份：`/root/aqc-deploy-backups/1.11.12-20260508221534/webroot-no-uploads.tgz`、`/root/aqc-deploy-backups/1.11.12-20260508221534/api-current.tgz`、`/root/aqc-deploy-backups/1.11.12-20260508221534/aqc-n-web-nginx.conf.bak`。

## 1.11.11 · 图片加速、销售图片与批量调拨单

- 复查商品图片上传/静态访问链路、商品管理图片列、销售记录列表与订单详情、商品分布弹层、工单分配和商品调拨单逻辑。
- 后端新增 `/api/goods/images/optimized` 图片优化读取接口，原始图片继续保留在上传目录；超过 1MB 的图片按需生成 `.optimized` 缓存 JPEG，并限制路径只能落在上传目录内。
- 前端新增 `apiOptimizedAssetUrl`，商品管理、录入销售和销售记录展示图片时统一走优化图片地址，避免直接加载大图。
- 销售记录接口补充 `goodsCoverImage` 与 `goodsImageList`，销售列表在型号后新增图片列并复用商品管理缩略图/预览样式；订单详情顶部新增整行商品图，详情字段补充数量。
- 商品分布标题改为只显示型号，副标题显示品牌/系列；移动端商品分布移除“有货点位 / 点位明细”统计条。
- 后端新增批量商品调拨草稿接口，使用现有工单与分配草稿表保存批量调拨矩阵，确认时按收货店铺生成多张商品调拨单草稿。
- 桌面端工单新建商品调拨单新增“批量商品调拨单”开关；开启后收货点位在下一步按顺序选择，明细表按“商品为行、店铺为列”录入变动库存和查看当前库存。

## 1.11.11 验证

- 已完成 Python 编译检查：`backend/fastapi/app` 通过。
- 已完成前端生产构建：`client/dist/version.json` 输出 `1.11.11`。
- 未进行前端模拟操作，按需求仅做代码级检查、编译与生产构建验证。

## 1.11.10 · 商品图片上传与移动端销售底栏修复

- 复查商品图片上传链路、Nginx 代理配置、Docker 持久化挂载、商品管理表格和移动端录入销售页面，确认 3.1MB PNG 的 `413` 是前置 Web 容器默认请求体限制导致。
- `client/nginx.conf` 增加 `client_max_body_size 16m`，并新增 `/uploads/` 只读静态访问、关闭目录索引和 `X-Content-Type-Options: nosniff`，让商品图片可由 Web 容器稳定访问。
- `docker-compose.yml` 将上传目录抽成 `AQC_UPLOAD_HOST_PATH`，API 和 Web 容器共享同一持久目录；生产部署时可指向版本目录之外，避免发布覆盖上传文件。
- 后端商品图片上传保留 8MB 业务上限，增加图片文件魔数校验和临时文件原子替换，避免伪造扩展名或半写入文件留在正式路径。
- 桌面端商品管理表格在属性列前新增图片列，缩略图保持 34px，不增加现有表格行高；点击缩略图可查看大图，无图显示 `-`。
- 商品管理桌面端商品列表新增固定表头，普通模式和对比模式滚动长列表时，型号、图片、属性和对比分组表头会常驻可视区域顶部。
- 移动端录入销售的信息步骤将“确认录入销售”改成独立固定底栏，并给内容区预留底部空间，滚动页面时按钮完整常驻底部。

## 1.11.10 验证与部署

- 已完成 Python 编译检查：`backend/fastapi/app` 通过。
- 已完成前端生产构建：`client/dist/version.json` 输出 `1.11.10`。
- 已完成本地 Playwright 渲染验证：商品管理普通模式和对比模式滚动后，表头 `headerTop` 均保持在内容区顶部；桌面端图片列有图可打开大图、无图显示 `-`；移动端录入销售确认按钮 Teleport 到页面底部后完整常驻。
- 已部署到服务器 `1.14.197.141`，线上 `https://aqc.symuse.com/version.json` 为 `1.11.10`，`/api/health` 返回正常。
- 已验证 4MB 请求体经 Web 容器转发到上传接口返回 `401 未登录`，不再被 Nginx 拦截为 `413`；`/uploads/` 响应头包含 `X-Content-Type-Options: nosniff`。
- 服务器备份：`/root/aqc-deploy-backups/1.11.10-20260508163307/webroot-no-uploads.tgz`、`/root/aqc-deploy-backups/1.11.10-20260508163307/api-current.tgz`、`/root/aqc-deploy-backups/1.11.10-20260508163307/aqc-n-web-nginx.conf.bak`。

## 1.11.9 · 商品图片管理与移动端销售录入细化

- 接续 1.11.8 的图片导入修复，先复查商品图片字段、商品管理移动端详情、商品编辑弹窗、销售记录订单详情、移动端录入销售和店铺排班接口。
- 商品管理移动端商品详情新增商品图片展示，有图片时在价格板块上方占整行显示，没有图片时不改变原详情布局。
- 商品编辑弹窗新增图片管理区，支持添加图片、设置主图和删除图片；保存时只更新商品的 `coverImage` 与 `imageList` 字段，不影响库存、销售和工单数据。
- 后端新增商品图片上传接口 `/api/goods/items/{id}/images/upload`，限制图片类型和大小，文件写入 `/uploads/goods/manual/{id}`，返回可访问的 `/uploads/...` 地址。
- 移动端销售记录的订单详情中，备注板块改为整行展示，减少长备注在两列网格内的挤压。
- 移动端录入销售的信息步骤将发货店铺移入“更多选项”；主行只保留销售员、销售店铺和更多按钮，销售员与销售店铺等宽，更多按钮高度对齐选择框。
- 移动端销售店铺和发货店铺选择框及菜单统一显示完整名称；新增“今日排班店铺”提示，当天无排班时显示所属店铺。
- 店铺排班摘要接口补充 `todayShifts` 字段，前端只读该字段用于展示，不参与销售记录提交。

## 验证与部署

- 已完成 Python 编译检查：`goods.py`、`shop_schedules.py`、`schemas.py`、`config.py` 均通过。
- 已完成前端生产构建：`client/dist/version.json` 输出 `1.11.9`。
- 已完成本地移动端自动化验证：商品详情图片位于价格板块上方、商品编辑图片管理可见并可上传、销售记录备注占整行、录入销售主行/更多选项/今日排班店铺提示符合要求。
- 已部署到服务器 `1.14.197.141`，线上 `https://aqc.symuse.com/version.json` 为 `1.11.9`，`/api/health` 返回正常。
- 已验证后端上传接口 `/api/goods/items/1008/images/upload` 返回成功；测试上传文件已删除，未写入商品、库存、销售或工单数据。
- 部署前后数据基线一致：商品 `6285`、带图商品 `980`、重复型号组 `0`、库存行 `2155` / 库存数量 `3145`、销售记录 `1376` / 数量 `1356` / 金额 `2252231.09`、库存日志 `4512`、工单明细 `2160`。
- 服务器备份：`/root/aqc-deploy-backups/1.11.9-web-20260508122114/webroot.tgz`、`/root/aqc-deploy-backups/1.11.9-api-20260508122114/api-current.tgz`；更新日志文案同步后补刷前端备份为 `/root/aqc-deploy-backups/1.11.9-web-logsync-20260508122735/webroot.tgz`。
