# 2026-05-09 开发日志

## 1.11.15 · 批量商品调拨单明细与点位选择修复

- 复查批量商品调拨单从工单大类入口、发货点位选择、收货点位选择、明细表格、连续新增栏、批量录入商品弹窗到草稿保存的完整链路。
- 将批量商品调拨单“选择收货店铺 / 仓库”重做为匹配功能同款点位按钮布局，保留点位点击顺序和已选数量摘要。
- 在批量商品调拨单明细页补齐普通商品调拨单同款连续新增栏，支持条码 / 商品名称继续快速录入。
- 在商品名称列新增“发货数量”输入框，将行发货数量和各收货点位分配数量拆开处理，并补齐后端草稿入参保存。
- 定位并修复新增一行、批量录入商品后页面卡死：`getBatchTransferQuantity`、`batchTransferRowAssignedQuantity` 不再在模板渲染期调用会重写响应式对象的初始化逻辑，新增行和批量带入商品时只初始化一次分配列。
- 顺手修复批量录入商品弹窗品牌 / 系列元数据为字符串数组时的 `ElOption` value 警告，并去掉只读 computed 列宽的无效赋值。

## 1.11.15 验证

- 已完成前端生产构建：`npm run build` 通过。
- 已完成后端语法检查：`python3 -m compileall -q backend/fastapi/app backend/fastapi/run.py` 通过。
- 已完成本地 mock API 渲染验证：桌面 1440 宽度下，批量商品调拨单可选择 2 个收货点位、进入明细、连续新增 2 行、打开批量录入商品弹窗、确认商品回填表格，并可在商品名称列编辑发货数量。
- 已完成深色模式入口验证：深色模式下批量商品调拨单“选择收货店铺 / 仓库”步骤正常渲染，未发现文字重叠或按钮样式异常。
- 浏览器插件连接超时后，改用本地 Playwright + Chrome 验证，截图保存在 `/tmp/aqc-batch-transfer-detail-light.png`、`/tmp/aqc-batch-transfer-after-batch-light.png`、`/tmp/aqc-batch-transfer-target-dark.png`。
- 已部署到服务器 `1.14.197.141`，线上 `https://aqc.symuse.com/version.json` 为 `1.11.15`，`https://aqc.symuse.com/api/health` 返回 `200 OK`。
- 已检查线上 API 容器日志，重启后工单、商品、通知等接口持续返回 `200 OK`；本次保留 `uploads`、`legacy-assets` 和 `.user.ini`。
- 服务器备份：`/root/aqc-deploy-backups/1.11.15-20260509102937/webroot-no-uploads.tgz`、`/root/aqc-deploy-backups/1.11.15-20260509102937/api-current.tgz`、`/root/aqc-deploy-backups/1.11.15-20260509102937/aqc-n-web-nginx.conf.bak`。

## 1.11.14 · 修复新建工单大类后编辑弹窗

- 复查工单管理“新建工单”入口、大类选择弹窗、桌面编辑弹窗、移动端工单流程和批量调拨收货点位渲染链路。
- 本地用 mock API 复现桌面端问题：点击“商品类工单”或“销售类工单”后，编辑弹窗渲染阶段抛出 `_ctx.formatShopType is not a function`，导致后续弹窗不显示。
- 补齐 `formatShopType`，收货点位类型按店铺、仓库、其他仓库、维修点显示，修复弹窗渲染错误。
- 将工单编辑器展示模式从单纯 viewport 判断改成显式 `editorPresentationMode`，从桌面大类弹窗进入时保持桌面编辑弹窗流程，移动端入口继续使用移动分步流程。
- 本次只修改工单前端展示与状态切换，不变动商品信息、销售信息和后端业务数据。

## 1.11.14 验证

- 已完成本地 mock API 渲染验证：1280 桌面宽度下，“商品类工单”和“销售类工单”均能打开桌面“新建工单”编辑弹窗，弹窗内显示“工单信息”。
- 已完成深色模式验证：1280 桌面宽度、系统深色模式下，“销售类工单”可正常打开编辑弹窗。
- 已完成前端生产构建：`client/dist/version.json` 输出 `1.11.14`。
- 已部署到服务器 `1.14.197.141`，线上 `https://aqc.symuse.com/version.json` 为 `1.11.14`，`/api/health` 返回正常。
- 已检查线上容器日志：Web 容器 reload 成功，API 健康检查 `200 OK`；部署清理阶段遇到受保护 `.user.ini` 后已保留该文件并重新覆盖静态产物，`uploads` 目录未清理。
- 服务器备份：`/root/aqc-deploy-backups/1.11.14-20260509095733/webroot-no-uploads.tgz`、`/root/aqc-deploy-backups/1.11.14-20260509095733/api-current.tgz`、`/root/aqc-deploy-backups/1.11.14-20260509095733/aqc-n-web-nginx.conf.bak`。
