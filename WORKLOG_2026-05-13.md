# 2026-05-13 开发日志

## 1.11.17 · 统一账号同步稳定修复

- 复查 AQC-N 管理员设置新增账户链路、`backend/fastapi/app/routers/admin.py` 中的 account-symuse 同步逻辑，以及 account-symuse 的登录、管理员用户、AQC 用户组接口。
- 线上排查服务器 `1.14.197.141` 容器环境与日志，确认 AQC-N 使用 `SYMUSE_ADMIN_ACCOUNT=17771891175` 登录 account-symuse 时返回 `账号或密码错误`，导致新增账户弹窗展示了错误的登录失败文案。
- 将 AQC-N 统一账号同步改为优先使用 `SYMUSE_CLIENT_ID / SYMUSE_CLIENT_SECRET` 受信服务调用 account-symuse 管理接口，减少对人工管理员账号密码状态的依赖。
- account-symuse 后端补齐内部 AQC 服务鉴权：仅持有匹配 AQC 客户端密钥的服务请求可调用用户管理与 AQC 用户组管理接口；客户端密钥轮换等更高权限接口仍保留管理员登录要求。
- AQC-N 新增账号中心账号冲突自动收口：当统一账号已按手机号、用户名或邮箱存在时，自动查找并绑定对应账号，更新资料、同步初始密码和 AQC 用户组，不再直接失败。
- AQC 用户组同步、账户资料更新、密码重置、账户停用继续走同一套 account-symuse 管理接口，避免新增 / 编辑 / 同步口径分叉。
- 本次未改动商品信息、库存信息和销售信息；没有新增可视样式，深色模式沿用现有主题变量。

## 1.11.17 验证

- 已完成 AQC-N 后端语法检查：`python3 -m py_compile backend/fastapi/app/routers/admin.py` 通过。
- 已完成 account-symuse 后端语法检查：`python3 -m py_compile backend/fastapi/app/deps.py backend/fastapi/app/routers/admin.py` 通过。
- 已完成 AQC-N 前端生产构建：`cd client && npm run build` 通过，`client/dist/version.json` 为 `1.11.17`。
- 已完成 account-symuse 前端生产构建：`cd client && npm run build` 通过，`client/dist/version.json` 为 `1.8.6-20260513-122853`。
- 已部署到服务器 `1.14.197.141`，线上 `https://aqc.symuse.com/version.json` 返回 `1.11.17`，`https://aqc.symuse.com/api/health` 返回 `200 OK`。
- 已同步 account-symuse `1.8.6` 后端与前端到服务器；因服务器 Docker 镜像代理拉取 `python:3.11-slim` 元数据失败，本轮先将源码与静态产物写入运行中容器并重启，线上 `https://account.symuse.com/version.json` 与 `https://account.symuse.com/api/health` 均校验通过。
- 已在 `aqc-n-api` 容器内使用 AQC 客户端密钥直接访问 account-symuse `/api/admin/users`，返回 `success=true`，确认内部服务鉴权生效。
- 已用 AQC 管理员本地登录创建临时账号 `19951303035`，返回“用户创建成功，已同步到 account AQC 用户组”，随后立即删除，确认新增账户不再出现“账号或密码错误”。
- 服务器备份：`/root/aqc-deploy-backups/1.11.17-20260513203035/` 与 `/root/account-deploy-backups/1.8.6-20260513203035/`。
